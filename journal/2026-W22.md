# 2026-W22

## 2026-05-26

**Tried.** First substantive thesis-side session after the W21 close-out. Resume from the post-01b position: re-derive the next-action ordering against what's actually been measured, then execute the highest-priority item. The cell-size sweep (01) settled geometry, the threshold sweep (01b) settled the suppression-rule operating point — both leave 02's variance-aware clearance field as the natural next contribution. Before opening 02, re-check 01's §Sim-phase validation suite for unfinished items that gate 02. Three: per-class reconstruction accuracy, α_d sensitivity, fast-yaw + splitter failure-mode. Audit each against 02's actual inputs: α_d sensitivity is structurally upstream of 02's δ_cal protocol (the doc names it as establishing the calibration pre-floor), per-class is reviewer-hygiene-not-02-gating, fast-yaw + splitter are scenario-world work and 01 itself ranks them low. So: run α_d first. Mid-day diversion when Oscar asked about the live-map figure showing only ~⅔ of the env (a follow-up on F4 from ADR 0006); answered the question, deferred the validation-figure infrastructure call until after the α_d result lands.

**Worked.**

- *Audit-first design pass on α_d.* Located the framework knob (`sensor_noise_factor` in `elevation_mapping_cupy/{parameter.py:162, kernels/custom_kernels.py:65}` — formula σ²_meas = α_d · z², production 0.05 on both floor and ceiling configs). Confirmed the `variance` layer is published by the ceiling node (`ceiling_complete.yaml:127`) but unread by `compute_ceiling_metrics.py`. Confirmed the ADR-0008 generalised runner accepts arbitrary axis names as direct YAML param overrides without runner changes — so adding α_d is a pure config+postprocess job.

- *Additive metric-script extension.* `compute_ceiling_metrics.py` extended with a new `_compute_variance_stats` and a parameter `variance_init_threshold` (default 50.0, half of `initial_variance: 100.0`). Cells with σ² above the threshold are flagged as still-initial (pinned near `initial_variance`, not yet carrying α_d signal); `init_dominated_frac` reports the fraction. Variance stats are written into both `summary.json` (top-level `variance`/`variance_core` blocks) and per-sample entries. Backward-compatibility verified: the elevation path is structurally unchanged, and a dry-run of 01b on the new code produces byte-identical rendered ceiling YAML to the archived live run (`threshold_sweep__c9d2b0bb724d_c0c90f3/rendered_configs/cs015_es1_wnt020.yaml`).

- *Runner extension.* Added `sensor_noise_factor` to `_LABEL_ENCODERS` (snf005/050/500) and pass-through of the variance JSON block into the per-cell `summary.json`. The runner doesn't otherwise change.

- *01c spec — α_d sweep at production geometry.* `sim_validation_01c/alpha_d_sweep.yaml` declares a 3-run matrix at `sensor_noise_factor ∈ [0.005, 0.05, 0.5]` (100× span around production, per 01 doc's "order of magnitude" prescription), cell=0.10 m + wnt=20 + es=true held at production, bag reused from 01/01b. The α_d=0.05 cell IS the reproducibility check against archived `joint_sweep__04a2e8802fc1_d57b109/cs010_es1` — sub-mm gate identical to 01b precedent. `acceptance:` block names the falsifiable prediction quantitatively: `variance_ratio_min=50, variance_ratio_max=200` for the 100× α_d span (linear-scaling prediction = 100×, ±2× tolerance for fusion-count variation). Above ⇒ extra variance source blowing up; below ⇒ framework clamps σ² independently of α_d.

- *01c postprocess.* `sim_validation_01c_postprocess.py` produces three blocks — reproducibility, variance response (ratio + verdict + 02 implication), elevation residual stability (spread check vs 5 mm tolerance).

- *Live sweep (26 m 44 s wall, ~26 min sweep + ~30 s overhead).* All three runs `status: ok`. Reproducibility PASS: RMSE_all Δ = +0.0 mm, RMSE_core Δ = +0.1 mm against archived cs010_es1. Sub-mm reproduction same as 01b — same-bag same-code same-params determinism holds at 5-day stretch. Elevation stability PASS: RMSE_core spread across α_d = 4.19 mm, below the 5 mm tolerance (Kalman gain shifts but equilibrium estimate doesn't — predicted behaviour, no finding).

- *Variance response FAIL (below), and it's the substantive finding.* Mean post-init σ² at α_d=0.005 → 0.05 → 0.5 measured as 0.0127 → 0.026 → 0.153. Ratio at the extremes: 0.153 / 0.0127 = **12.08× for a 100× α_d span**. Below the 50× lower acceptance gate. The framework's published per-cell variance does **not** scale linearly with the LiDAR noise-model parameter in this regime. The α_d knob moves σ² by only ~12% of what the noise model alone predicts; the remaining ~88% of the variance is contributed by sources that do not respond to α_d (additive floors, outlier-rejection short-circuits, drift coupling, or the Kalman fusion saturating somewhere — not yet diagnosed). For 02: the published variance pipeline is not a clean readout of LiDAR noise; δ_cal has more work to do than the original 02 doc accounted for. This shifts the calibration design — the structural premise that 02 §Calibration protocol implicitly leans on (that σ²_published is the LiDAR-noise channel and δ_cal is a small offset on top) needs revisiting.

- *0% init-dominated across all three runs.* The bag is long enough that every visible cell has settled to steady-state under all α_d values. Eliminates the "under-measured cells inflating the lower-α_d denominator" confound that could have produced an artificially low ratio.

- *Plain-language conversation discipline saved to memory.* `feedback_plain_language.md` — in conversation (not thesis docs) explain jargon in one line before using it, lead with the picture not the abstraction, match the scope of the answer to the scope of the question, don't extrapolate a small question into infrastructure design without checking. Triggered by Oscar's mid-day flag that working-conversation register had drifted academic; cost is comprehension while learning. Distinct from `feedback_writing_style.md` (which governs thesis-facing docs and stays compressed/rigorous).

**Didn't.**

- *Did not investigate the 12× variance clamp.* Identified as the natural follow-up; suspect parameters in scope are `outlier_variance: 0.01`, `mahalanobis_thresh: 2.0`, `max_variance: 100.0`, and the Kalman fusion math under high σ²_meas. Queued as a parallel-agent task spawned alongside this entry — the diagnostic is read-only on the existing 01c data plus targeted micro-experiments on the framework code, doesn't touch the experiment runner, and is bounded enough to bound-check in isolation.

- *Did not implement snapshot stitching for the full-env validation figure.* Oscar's mid-day question about the live-map figure showing only the rolling window surfaced this as a real validation-clarity question. Three options laid out (bigger map, snapshot stitching, `map_accumulator` package). Stitching looks the right answer — represents the deployed system honestly (rolling window unchanged) while showing the cumulative reconstruction across the bag. Decision deferred: revisit after the variance-clamp investigation lands, weigh the writing-up value against the implementation cost (~30 min coding + ~25 min re-run). The question isn't whether it's doable; it's whether the thesis actually needs the full-env figure for the colloquium-stage argument.

- *Did not write a new ADR for 01c.* The experiment is a direct application of the existing 01/01b family using ADR-0008's generalised runner. The substantive finding (variance clamp) wants its own ADR after the diagnostic lands and the 02-side response is decided — not before.

- *Did not chase the per-class breakdown.* Earlier extrapolation from the F4 figure question into a per-class infrastructure design was the wrong scope (Oscar correctly flagged this). The construction_site world has no slanted ceilings, so the 01 doc's "flat / slanted / with-overhead-features" taxonomy doesn't fit either — the doc text needs updating either way, but that's a small edit, not an infra build. Per-class becomes load-bearing only if a reviewer asks for it. Not on the path now.

**Surfaced.**

- *01's §Sim-phase validation suite has a taxonomy bug.* The doc names "flat / slanted / with-overhead-features" as the per-class partition. The actual `construction_site.world` has no slanted ceilings — the primitives are large flat construction surfaces (`construction_site_ceiling`, `hallway`, `slab_with_holes` flat tops), low-clearance suspended features (`slab_with_holes` lowered bottom, `H_beam_dynamic`, `hanging_pipe_long`, `cable_protector`), and point fixtures (`ceiling_lamp`). The doc should be updated to the actually-applicable taxonomy before any per-class work lands — small edit, no code.

- *F4's effect on the validation figure is more load-bearing than the W21 audit recorded.* ADR 0006 §F4 noted that the 20×20 m rolling map vs 40×30 m GT makes coverage non-interpretable. The deeper consequence — that the figure shows only the local view, never the cumulative reconstruction, even after a full-bag traversal — wasn't surfaced explicitly. Naming it as a separate validation-figure question (not just a coverage-number caveat) opens the snapshot-stitching path.

- *The α_d FAIL is a "we know the gap, design around it" outcome, not a "the framework is broken" outcome.* The reconstruction itself is α_d-insensitive (RMSE flat), the σ²-mean responds (12× ≠ 0×), the reproducibility is sub-mm — the framework is internally consistent. What it does is publish a per-cell σ² that conflates the α_d-driven channel with something else. For 02, this means δ_cal calibrates the full residual gap on the partner facility, not just the LiDAR mis-specification. The calibration-pre-floor question 01c was supposed to answer — "how much of the residual variance does the model already capture" — answered: ~12% of what α_d alone would predict at the extremes. δ_cal carries the rest.

**Changed.**

- `elevation_mapping_cupy/scripts/compute_ceiling_metrics.py` — additive `variance`/`variance_core` blocks; new parameter `variance_init_threshold` (default 50.0); per-sample variance entries; one-line variance summary in the per-sample log line. Elevation-side path unchanged.
- `src/thesis/experiments/runners/sim_validation_01_joint_sweep.py` — `sensor_noise_factor` added to `_LABEL_ENCODERS`; per-cell `summary.json` now pulls `variance`/`variance_core` from the metric JSON.
- `src/thesis/experiments/configs/sim_validation_01c/alpha_d_sweep.yaml` (new) — 3-run α_d sweep config with quantified acceptance gates (`reproducibility_tolerance_m=0.005`, `variance_ratio_min=50`, `variance_ratio_max=200`, `elevation_rmse_spread_tolerance_m=0.005`, `variance_metric=post_init_mean`).
- `src/thesis/experiments/configs/sim_validation_01c/README.md` (new) — falsifiable prediction, falsification cases, operating point, read-out, follow-ups.
- `src/thesis/experiments/runners/sim_validation_01c_postprocess.py` (new) — reproducibility / variance-response / elevation-stability blocks, idempotent append to `sweep_summary.md`.
- `src/thesis/experiments/results/sim_validation_01c/alpha_d_sweep__c5f4d00d1572_1b3b5e8/` — first measurement. Three runs, PASS / FAIL / PASS on the three blocks. The FAIL is the finding.
- `src/thesis/docs/01_dual_elevation_mapping.md` — implementation-status section updated with the 01c result and what it implies for 02.
- `~/.claude/projects/.../memory/feedback_plain_language.md` (new) — conversation-language discipline.
- `~/.claude/projects/.../memory/project_planned_packages.md` — refreshed to record that `hilda_clearance_field/`, `hilda_nmpc/`, `hilda_irm/` are now scaffolded (ament Python skeletons), with the package-boundary ADR still open. Was stale ("not yet scaffolded as of 2026-05-19").
- `src/thesis/journal/2026-W22.md` — this entry.

The 01c arc closes the geometry → threshold → noise-model triad on 01's sim-phase validation. Three measurements grounded in the same persistent bag, same reproducibility discipline, three different parameter axes. Two operating-point findings (cs/wnt = 0.10 m / 20 stay at production), one structural finding (the variance pipeline isn't clean α_d in this regime). 02's δ_cal design now stands on data, not on the original optimistic premise.

## 2026-05-26 (afternoon, diagnostic resolution + doc propagation)

**Tried.** Two streams in parallel after the 01c FAIL landed: (a) spawn a parallel agent on the diagnostic — read the elevation_mapping_cupy framework code, identify what's compressing the variance response from 100× to 12×, reconstruct the measured numbers from code-level mechanisms, and write the finding as ADR 0009. (b) Plain-language design discussion on a tangential question (validation figure scope vs deployed-system rolling window), then revert to the diagnostic stream and propagate ADR 0009 through 01 and 02.

**Worked.**

- *ADR 0009 — variance-pipeline-α_d-diagnostic.* Parallel agent landed the diagnosis on a fresh context using only the FAIL block, code paths, and a brief on the disciplines. Mechanism: the published σ² is the steady-state balance of Kalman draw-down (`add_points_kernel` `custom_kernels.py:150-283` — harmonic-mean batch update `new_v = map_v · v / (map_v + v)` not per-point sequential) against constant per-second inflation (`update_variance` `elevation_mapping.py:480-482` — `map_v += time_variance` every `1/update_variance_fps` s). The recurrence with inflation between batches has fixed point `map_v* = (-Δ + √(Δ² + 4vΔ)) / 2`, which in the production regime (v ≫ Δ) reduces to **`map_v* ≈ √(vΔ)`**. Predicted ratio across the 100× α_d span: √100 = 10×. Measured: 12.08×. The 2× excess is the pre-floor tail at α_d=0.5 — cells with observation count below `K_cross ≈ √(v/Δ) ≈ 150` are still in the linear-draw-down regime where map_v ≈ v/K, and lift the mean. Full algebraic reconstruction matches the per-α_d measured numbers (mean and median) within a factor of 2 across the swept range; the diagnosis is grounded in code citations + closed-form recurrence, not pattern-matching. Spot-verified the load-bearing line references against the actual code (`elevation_mapping.py:482`, `custom_kernels.py:179-181`, the harmonic-mean update at 187-188) — all match exactly. The agent also flagged a quirk in the outlier branch: the Mahalanobis gate uses `map_v · thresh` not `√map_v · thresh`, so the parameter name `mahalanobis_thresh` invites the wrong mental model — true Mahalanobis would scale with σ, not σ². Worth flagging in framework-side reasoning.

- *Suspect-parameter resolution.* F4 (outlier_variance, 0.01): bounded contribution — `atomicAdd(&map[1], outlier_variance)` is overwritten by `average_map_kernel` whenever any inlier lands in the same batch, so it survives only for all-outlier batches. F5 (max_variance, initial_variance, drift_compensation_variance_inler): all ruled out by code path — max_variance never binds (observed max σ² at α_d=0.5 ≈ 4.5 vs cap 100), init-dominated 0 %, drift compensation flag off. The √-floor is the dominant mechanism; outlier_variance is a small contributor at low α_d that lifts the floor by ~0.01 m².

- *Recovery options A/B/C ruled out, D accepted.* A (zero time_variance), B (raise update rate), C (zero outlier_variance) all trade well-understood semantics (staleness signal, outlier visibility) for cosmetic α_d-linearity. D — accept the floor regime and re-scope δ_cal — is the recommended choice. The framework's regime is the deployment regime; calibration adapts to it, not the other way round.

- *Corroborating experiment spec — `sim_validation_01d/time_variance_sweep`.* Falsifiable prediction: σ² ∝ √(time_variance) at fixed α_d. Four-cell sweep over time_variance ∈ {0, 0.0001, 0.001, 0.01} at production α_d=0.05, cs=0.10, wnt=20, es=true. The tv=0.0001 cell IS the in-sweep reproducibility check against 01c snf050 (same params + same bag + same code). Three pairwise σ² ratios on the non-zero cells tested against √-prediction with ±50 % tolerance; zero-cell tests the linear-only limit (predicted σ²(tv=0) ≪ σ²(tv=0.0001)). Runner extended with `time_variance` axis encoder (`tv00000`/`tv00100`/`tv01000`/`tv10000` — int*1e6 padded); postprocess script wrote and dry-run-verified; the 4 rendered ceiling YAMLs carry the right `time_variance` per cell. Ready to launch (~36 min wall).

- *Diagnostic-spot-verification (audit-before-trust).* The agent ran in a fresh context with no track record; before committing the diagnosis to 01 and 02, spot-checked the two most load-bearing claims against the actual elevation_mapping_cupy code (the inflation line, the outlier branch). Both match exactly. Audit-before-measure applies to agent outputs too — the agent's reasoning is faithful to the code, but verification is cheap and protects against silent miscitations.

- *Plain-language conversation discipline applied mid-stream.* The mid-day "why does the figure only show part of the env" question was answered narrowly (rolling window, last-snapshot) without extrapolating into per-class infrastructure design, after the user flagged that earlier dense-jargon answers were hard to follow. New memory entry [[feedback-plain-language]] captures the rule for future sessions: explain jargon in one line before use, lead with picture not abstraction, match scope, don't extrapolate small Qs into infrastructure designs. Distinct from [[feedback-writing-style]] (thesis docs stay compressed/rigorous in published form; the rule is about working conversation).

- *Documentation propagation per ADR 0009 §Documentation propagation.* All five edits landed:
  - `sim_validation_01c/alpha_d_sweep__c5f4d00d1572_1b3b5e8/sweep_summary.md` — appended diagnostic resolution + ADR link so the FAIL verdict reads with context.
  - `thesis/docs/01_dual_elevation_mapping.md` §Implementation status — added the 2026-05-26 diagnostic-resolution block (mechanism, 02 implication, 01d corroborating-experiment pointer, validation-figure-vs-full-env-extent caveat) directly under the 01c block.
  - `thesis/docs/02_variance_aware_clearance.md` §Theory — replaced the implicit `σ²_published = σ²_meas` reading with the √-floor expression and the three-distinct-gaps decomposition (√-compression, within-cell geometric spread, time-inflation contribution). The previous three-shortfall list is superseded; the new list is structurally distinct and traceable to the diagnostic.
  - `thesis/docs/02_variance_aware_clearance.md` §Calibration protocol — added the freshness-conditional binning step (≤ 0.5 s, 0.5–2 s, 2–5 s, > 5 s bins keyed off the published `update_time` layer), the operating-point lock (`time_variance` and `update_variance_fps` committed to the calibration manifest; runtime audit refuses to publish `f` if the live config diverges), and a second ablation (single-bin fit vs per-bin lookup).
  - `thesis/docs/02_variance_aware_clearance.md` §Open questions — closed "calibration pre-floor" with the ADR reference; opened "per-cadence δ_cal vs single constant" pending the partner-facility corpus; partially closed "suppression-firing rate" by linking the 01b result and the ADR's consequence that the dense/sparse corpus split is now load-bearing rather than contingent.

**Didn't.**

- *Did not launch 01d in this session.* The user is launching it themselves, same pattern as 01c. The spec, runner extension, and postprocess script are ready; dry-run rendered the right configs. Launch deferred to the user's command.
- *Did not implement snapshot stitching for the full-env validation figure.* The plain-language discussion settled on "skip for now, add a caption-paragraph clarification instead" (zero coding); the rolling-window caveat is now in 01's implementation-status section. If a future writing-up phase needs the cumulative-reconstruction figure for the thesis report (the user has explicitly stated no colloquium-figure pressure on this), the stitching infra is ~30 min and lands then.
- *Did not also implement the floor + ceiling side-by-side figure that I floated as an alternative.* User correctly observed this is overkill before there's an actual reviewer asking for it. Defer until needed.

**Surfaced.**

- *Audit-before-trust applies to agent outputs as a special case of audit-before-measure.* A fresh-context agent has no inherited track record; treating its outputs as authoritative without spot-verifying the most load-bearing citations would be the analogue of consuming a measurement without auditing the data path. Spot-checking the two key line references took ~30 s and would have caught any silent miscitation. Add to the audit-before-measure pattern: when a downstream consumer (a doc rewrite, a calibration design) commits to an agent's diagnosis, verify the agent's code-citations exist and read as the agent claims before propagating.
- *The diagnostic was producible from code alone in ~1 h.* The ADR's discipline note flags this: the assumption that σ²_published is the LiDAR-noise model was inherited from Fankhauser 2018 §III-B without checking how Miki 2022 §II-C diverged via the `time_variance` increment. The primary-source-reads pattern needs a complement: when a downstream design assumes a published layer is "the X model," verify the recurrence the code actually implements before treating the conceptual source as authoritative. This is the [[feedback-primary-sources]] complement on the runtime-interface side.
- *Module 02's δ_cal scope grew, not shrank, from the diagnostic — and that's good news.* The original 02 doc had δ_cal absorbing three specific shortfalls (sensor-model under-reporting on texture-poor surfaces, unmodelled localisation drift, contingent mean-vs-min bias). The diagnostic replaced that list with three structurally distinct gaps (√-compression, within-cell geometric spread, time-inflation contribution) — same calibration object, sharper accounting, with the operating-point lock and freshness-conditional binning making the calibration auditable. The "calibration carries more than I thought" landing is structurally healthier than "the framework is broken in some unspecified way."
- *The deployment-cadence-conditional dependency makes the controller-family decision (NMPC vs HMPC) slightly less neutral than before.* The freshness-conditional binning requires the runtime to look up Δt per cell from the published `update_time` layer, which adds a small per-query cost in the constraint-field evaluation. NMPC at 10 Hz reads ~few-thousand cells per horizon roll-out; HMPC's hybrid mode roll-outs might hit more cells per cycle. Not a decisive consideration but worth recording — the controller-family decision is still open per [[project-open-decisions]], and this is a minor weight on the NMPC side. Not enough to pre-commit either way.

**Changed.**

- `src/thesis/docs/decisions/0009-variance-pipeline-alpha-d-diagnostic.md` (new) — ADR by parallel agent, spot-verified, accepted. Findings F1–F5, quantitative reconstruction, recovery options A–D (D accepted), implication for 02, follow-up experiment proposal.
- `src/thesis/docs/02_variance_aware_clearance.md` — §Theory rewritten to replace `σ²_published = σ²_meas` with the √-floor expression and the three-distinct-gaps decomposition; §Calibration protocol gained the freshness-conditional binning step, the operating-point lock, and a second validation ablation; §Open questions closed "calibration pre-floor" (with ADR link), opened "per-cadence δ_cal vs single constant," partially closed "suppression-firing rate."
- `src/thesis/docs/01_dual_elevation_mapping.md` §Implementation status — added the 2026-05-26 diagnostic-resolution block (mechanism, 02 implication, 01d pointer, validation-figure-vs-full-env caveat) under the 01c block.
- `src/thesis/experiments/results/sim_validation_01c/alpha_d_sweep__c5f4d00d1572_1b3b5e8/sweep_summary.md` — appended diagnostic resolution + ADR link.
- `src/thesis/experiments/configs/sim_validation_01d/{time_variance_sweep.yaml,README.md}` (new) — 4-run corroborating experiment with quantified √-scaling acceptance gates.
- `src/thesis/experiments/runners/sim_validation_01_joint_sweep.py` — `_LABEL_ENCODERS` extended with `time_variance: tv{int(round(v * 1e6)):05d}`.
- `src/thesis/experiments/runners/sim_validation_01d_postprocess.py` (new) — four-block postprocess (reproducibility, √-scaling, zero-inflation, elevation stability).
- `~/.claude/projects/.../memory/feedback_variance_layer_regime.md` (new, by parallel agent) — the √-floor finding as a memory entry future sessions consume before reading 02.
- `src/thesis/journal/2026-W22.md` — this entry.

The diagnostic arc closes the 01c FAIL into a 02 design update + a corroborating experiment, all in one afternoon. The G2 module premise is now grounded in what the framework actually publishes, not in what the conceptual source paper would suggest. Next: launch 01d, then move to the package-boundary ADR for `hilda_clearance_field` (the last remaining gate before phase-2 work on the variance-aware kernel starts).

## 2026-05-26 (evening, 01d empirical update + 02 review-driven corrections)

**Tried.** Two work-streams in parallel after Oscar ran 01d and circulated a four-point review of the updated 02 doc from a second agent: (a) read 01d's actual results and integrate the empirical finding back into ADR 0009 + the doc chain, (b) evaluate each of the four 02-review points on its own merit (Oscar explicitly framed them as "hints not gospel") and apply only the ones that survive verification.

**Worked.**

- *01d result — partial confirmation of ADR 0009 + one substantive empirical surprise.* Pairwise σ² ratios across the three non-zero `time_variance` cells all PASS the ±50% √-tolerance: 1.73× / 4.41× / 7.63× against predicted 3.16× / 3.16× / 10× across the 10× / 10× / 100× `time_variance` ratios. The √-mechanism is empirically present in the form predicted by ADR 0009 §F2. **But the zero-inflation check FAILED hard**: σ² at `time_variance=0` measured 0.0244 m² vs 0.0268 m² at production `time_variance=0.0001` — a 9% drop against an acceptance gate of 50%. The √-floor is real but it is **not what sets the published σ² at the production operating point**. A second α_d-independent source contributes ≈ 0.024 m² regardless of `time_variance`, with the √-floor only taking over at `time_variance ≥ 0.01` (~100× production). Most likely production-regime contributor: the outlier-injection branch flagged in ADR 0009 §F4 (originally called "bounded contribution"; empirically dominant). The combined picture is partial-confirmation, not clean PASS — the auto-generated postprocess verdicts (√-scaling PASS + zero-inflation FAIL) needed a hand-written reconciliation note in the sweep summary because reading them independently suggested a cleaner outcome than the data supports.

- *Discipline lesson surfaced from 01d: code-level diagnostics need empirical confirmation before doc propagation commits to specific mechanisms.* ADR 0009's §F4 ("bounded contribution") was code-correct (the outlier injection IS bounded by the overwrite logic in `average_map_kernel`) but loose on magnitude (the bound is reached in production, making it the dominant contributor). The √-mechanism was internally consistent, faithful to the recurrence, and *one of* the contributors — but not the production-dominant one. Without the empirical sweep, 02's δ_cal protocol would have committed to a calibration manifest that locked the wrong parameters (`time_variance`, `update_variance_fps`) without `outlier_variance` and `mahalanobis_thresh`. The empirical sweep cost ~36 min and corrected the lock-list before the calibration corpus is collected on the partner facility. Partial-confirmation is the most common outcome of this kind of code-level reasoning, not full PASS / full FAIL — the right protocol is to record what was confirmed, what was under-specified, and what would falsify further hypotheses, not to retro-fit the original ADR into a clean victory.

- *Review-point evaluation against the `superpowers:receiving-code-review` skill.* All four points evaluated with technical verification before implementing:

  - **A (one-sidedness inconsistency) — verified, real bugs.** Two instances: (i) §Theory said "λ=3 corresponds to a 99.7% probability that the true clearance exceeds..." — but 99.7% is the two-sided `P(|Z|<3) = 2Φ(3)−1` from the empirical rule, not the one-sided coverage. At λ=3 the one-sided `P(c_true > ĉ - margin)` is `Φ(3) ≈ 99.865%`. (ii) §Calibration protocol's objective `|P(|z_ref − ẑ_ceil| > λ√σ² + δ_cal) − Φ(−λ)|` uses two-sided absolute-value exceedance but targets one-sided `Φ(−λ)` — for a symmetric residual this fits δ_cal to twice the dangerous-tail mass and over-margins by half a tail. Fixed by rewriting the prose to the correct one-sided coverage figure and rewriting the objective on the one-sided lower-tail event `P(r < −(λ√σ² + δ_cal))` where `r = z_ref − ẑ_ceil` and the dangerous case is `ẑ_ceil > z_ref` (estimated ceiling above true ceiling → less clearance than the planner thinks).

  - **B (ε behavioural claim contradicts the diagnostic) — verified, real issue.** The doc said ε tightens "over geometrically complex overhead features (ducts, beams)." But ADR 0009 establishes that the framework's per-point noise model `α_d · rz²` is distance-only — it does not see local curvature or within-cell planar deviation. A beam's near face is **closer** to the sensor (smaller `rz`), so σ²_meas drops there and ε relaxes. The within-cell geometric spread that would matter physically is routed into δ_cal as a non-spatial constant offset, which over-margins flat interiors to cover edges (defensible conservatism, but not the same as the "tracks complexity" claim). Fixed by rewriting the behavioural-claim paragraph to state clearly that σ²_published tracks **sensing confidence** (range, cadence, incidence, outlier rate) and not feature complexity, with the geometric handling routed via `c` (the elevation channel via the suppression rule) not ε. The "if a future iteration needs geometric selectivity, a roughness/normal-residual layer would supply it at the cost of a new perception channel" tradeoff is named explicitly rather than hidden.

  - **C (framing should be conformal, not Hewing/Lorenzen) — verified, sharper claim.** Hewing 2020 / Lorenzen 2016 derive the constraint tightening from a faithful propagated covariance. The construction here cannot do that because σ²_published is the √-compressed Kalman+inflation state, not the LiDAR-noise covariance. The δ_cal fit on residuals to match empirical lower-tail mass to `Φ(−λ)` is **split-conformal calibration** of the additive offset — fitted on a held-out corpus, generalisation via exchangeability. Fixed by adding a "Calibration class" paragraph that names the hybrid (chance-constraint functional form + split-conformal coverage guarantee), distinguishes it from both Hewing/Lorenzen tube MPC and from pure split-conformal prediction (which wouldn't use the published σ² shape), and reframes the contribution claim. Stronger ICRA framing and more honest about what the construction actually delivers.

  - **D (δ_cal corpus scope drift) — verified, clarity issue.** Theory's δ_cal targets composed `σ²_c = σ²_zceil + σ²_zfloor`; protocol's objective uses ceiling-only `σ²_zceil`. At HILDA's geometry (floor returns short-range, near-normally-incident, dense; ceiling returns longer-range and more oblique) `σ²_zfloor ≪ σ²_zceil` is defensible so the ceiling-only fit approximately covers the composed margin — but the assumption was silent and the floor's own √-compression rides into deployment uncalibrated. Fixed by adding a "Scope of the calibration" paragraph that states the working assumption explicitly, names the threshold for triggering an independent floor-side calibration (`σ²_zfloor` within ~30% of `σ²_zceil` on any surface class), and adds a verification step (`σ²_zfloor` recorded per cell in the corpus) so the assumption is tested on the partner-facility data when it arrives.

- *Audit-before-trust applied to the second review agent's outputs as a generalisation of the morning's pattern.* Each of the four points was verified against the doc text and against the framework code (where applicable) before being accepted. All four survived verification — they're genuinely substantive. No performative agreement; no blind implementation. The skill's "verify before implementing, push back with technical reasoning if wrong, technical acknowledgment when correct" pattern is now the operating discipline for both code reviews and ADR reviews.

**Didn't.**

- *Did not run an outlier_variance sweep to verify the production-regime mechanism attribution.* The 01d FAIL strongly suggests outlier-injection as the dominant contributor, but the chain of reasoning (Mahalanobis gate rejects more points at high observation density → more outlier injections survive in all-outlier batches → cumulative floor) is hypothesis-level, not measured. Diagnostic sweep `outlier_variance ∈ {0, 0.001, 0.01, 0.1}` would pin it down (~36 min on the same bag). Not on the critical path — 02's calibration design accommodates the gap regardless of its specific source, and the four-parameter lock list (`time_variance`, `update_variance_fps`, `outlier_variance`, `mahalanobis_thresh`) covers the contributors regardless of which dominates. Worth running later if the partner-facility calibration produces residuals that don't match the production-bag pattern.

- *Did not push back on any of the four review points.* All four survived verification. If any had been ambiguous or wrong-for-this-codebase, the receiving-code-review skill calls for technical pushback with reasoning. None were.

- *Did not move to the package-boundary ADR for `hilda_clearance_field`.* That was the planned-next-after-01d item; the 01d empirical surprise + the 02 review-driven corrections consumed the evening. Boundary ADR carries over to next session.

**Surfaced.**

- *Partial-confirmation is the most useful empirical outcome and the hardest to record honestly.* Both ADR 0009 (which was algebraically correct + production-regime-misweighted) and the auto-generated 01d postprocess (which produced a √-scaling PASS that, read alone, suggests the diagnosis is fully confirmed) need active reconciliation work to land the actual finding correctly. The skill failure-mode here is reporting "PASS" + moving on; the disciplined failure-mode is reporting "PASS but...", reconciling against the FAIL elsewhere in the same sweep, and updating the prior diagnosis to reflect what's confirmed AND what's still open. The reconciliation note in 01d's sweep_summary and the ADR's §Empirical update both do this; the discipline is to keep doing it on subsequent partial-confirmations rather than retrofitting any of them into clean victories.

- *Postprocess scripts should support reconciliation between PASS/FAIL across separate checks.* The current 01d postprocess writes the four blocks independently. The √-scaling PASS message ("ADR 0009 §F2 is on solid empirical footing") is technically correct in isolation but misleading in combination with the zero-inflation FAIL. A small enhancement to the postprocess would auto-detect that the √-scaling result + zero-inflation result are jointly diagnostic of partial confirmation and emit a unified verdict. Not urgent — by-hand reconciliation note suffices for this sweep — but the pattern recurs whenever a script computes independent acceptance gates that combine into a non-independent diagnosis.

- *The two-agent workflow (diagnostic agent + review agent) is producing substantive corrections that don't get caught by single-agent or human-only review.* The diagnostic agent's ADR 0009 read as confident at submission; the empirical sweep + a separate review agent each surfaced corrections the diagnostic agent would not have surfaced alone. Worth recording as a working pattern: when a diagnostic produces a substantive doc rewrite, queue both an empirical confirmation pass AND an independent review pass before propagating, not either-or.

- *The receiving-code-review skill is doing real work.* All four points were technically substantive; none were nit-picks. Treating them with technical-rigor-not-performative-agreement avoided two real statistical bugs (one-sidedness in two places), a behavioural claim that contradicted the diagnostic the doc itself committed to, a framing claim that overstated kinship with literature it doesn't satisfy, and a silent corpus-scope assumption. The cost of taking each point at its word would have been four real defects in the calibration deliverable. Conversely, the cost of treating them as orders-to-follow-blindly without verification would have been the same defects + lost technical understanding. The skill's audit-each-point-against-the-codebase pattern is what produced the right outcome.

**Changed.**

- `src/thesis/docs/02_variance_aware_clearance.md` §Theory — four edits: (i) replaced "99.7%" with one-sided "99.865%" and added the empirical-rule clarification; (ii) added the "Calibration class" paragraph naming the construction as a hybrid (chance-constraint functional form + split-conformal coverage guarantee); (iii) rewrote the ε-behaviour paragraph to track sensing confidence rather than feature complexity, with the geometric-selectivity tradeoff named explicitly; (iv) the "interpretation of `λ √σ²_c`" paragraph at the end of §Theory still stands as written this morning.
- `src/thesis/docs/02_variance_aware_clearance.md` §Calibration protocol — two edits: (i) added "Scope of the calibration — ceiling-only as a working assumption" paragraph with the verification step; (ii) rewrote the objective on the one-sided lower-tail event with the directional semantics (`r = z_ref − ẑ_ceil`, dangerous case `ẑ_ceil > z_ref`).
- `src/thesis/docs/decisions/0009-variance-pipeline-alpha-d-diagnostic.md` — added §"Empirical update — sim_validation_01d result" with the pairwise-PASS / zero-FAIL findings, the mechanism-attribution shift to outlier-injection at production cadence, the revised consequences (calibration manifest gains `outlier_variance` + `mahalanobis_thresh` as locked parameters), and the discipline-takeaway (code-level diagnostics need empirical confirmation pass before doc propagation; partial-confirmation is the most common outcome).
- `src/thesis/docs/01_dual_elevation_mapping.md` §Implementation status — rewrote the "corroborating experiment queued" paragraph as "corroborating experiment complete — partial confirmation" with the two findings and the mechanism-attribution shift.
- `src/thesis/experiments/results/sim_validation_01d/time_variance_sweep__9d153d07e4dd_1b3b5e8/sweep_summary.md` — appended hand-written Reconciliation note that reads the four auto-generated verdicts as a combined diagnosis rather than independent gates.
- `~/.claude/projects/.../memory/feedback_variance_layer_regime.md` — rewrote: description and body now reflect that the published σ² at HILDA production is dominated by outlier-injection (≈ 0.024 m² floor), with the √-floor of time_variance only becoming dominant at ~100× production. The original mechanism (Kalman-vs-time_variance √-floor) is recorded as one of the contributors with its empirical regime named.
- `src/thesis/journal/2026-W22.md` — this entry.

The 02 doc now stands on a verified statistical foundation (one-sided coverage, split-conformal framing, ceiling-only scope-named), a verified behavioural description (ε tracks sensing confidence not feature complexity), and a verified empirical mechanism attribution (outlier-injection at production cadence, √-floor as fallback at much higher `time_variance`). Three verification streams (the code-level diagnostic, the empirical sweep, the independent review) produced corrections the original single-agent draft would not have produced. Next: package-boundary ADR for `hilda_clearance_field`.

## 2026-05-26 (late evening, package-boundary call)

**Tried.** Last gate before phase-2 variance-aware kernel work: decide where the code lives. Two homes on disk — `hilda_ceiling/ceiling_constraint_field/` (working C++ skeleton, in the metapackage, sibling DDS consumers already targeting `/constraint_field`) and `hilda_clearance_field/` (empty top-level ament_python scaffold THESIS.md commits to). Three real options: A absorb into the top-level package, B split by role, C delete the top-level and centralise.

**Worked.**

- *Audit of the actual artefacts before deciding which directory wins.* Read the existing C++ skeleton, the metapackage manifest, the three sibling configs (`ceiling_height_lookahead`, `ceiling_collision_monitor`, `ceiling_controller` — all carry `constraint_field_topic: "/constraint_field"` in their config defaults), THESIS.md's package-layout commitment, and the 02 doc's package-boundary-tension paragraph. The substantive boundary turns out to run between two *artefacts*, not two domains: the runtime publishing node (10 Hz GridMap, on-GPU fused kernel, DDS-consumed by ceiling-pipeline siblings) and the controller-facing interface (CasADi B-spline export, δ_cal calibration scaffolding fitted once before deployment, Python/CuPy sim-phase prototype). Different rebuild cadences, different build systems, different consumers.

- *Option B chosen and recorded as ADR 0010.* Keep the C++ runtime node where its DDS siblings expect it; narrow `hilda_clearance_field/` to the three controller-facing deliverables (spline export, δ_cal scaffolding, prototype kernel). Controller packages depend on `hilda_clearance_field` for what they consume in-process; the publishing node is reached transitively through the `hilda_ceiling` metapackage. The naming friction is acknowledged in the ADR (the package no longer hosts the field *computation*, only the *interface* to it) with the `ros2_numpy` analogue as precedent. Flip condition recorded: if G5 measurements show the publication overhead dominates the controller's interface latency, the boundary dissolves and the spline export migrates into the runtime node — named so the call can be re-opened against measurement rather than re-derived.

- *Decision propagation in one pass.* 02 §Implementation status's "Package boundary tension" paragraph rewritten as "Package boundary settled" with the role split spelled out and the ADR linked. The "Package boundary" open question struck through with the *Closed 2026-05-26* annotation and an ADR pointer. `hilda_clearance_field/package.xml` and `setup.py` descriptions narrowed to the controller-facing-interface scope (the previous descriptions overstated the package's role by listing the GPU kernel, which lives in `ceiling_constraint_field` under Option B).

- *Audit-before-trust applied to the boundary call itself.* The first read of THESIS.md's "Kept separate from `hilda_ceiling` because both NMPC and HMPC will consume from it" pulled toward Option A (absorb the C++ node). Verifying against what NMPC/HMPC actually consume in-process — the CasADi B-spline + calibration manifest, not the `/constraint_field` topic directly — rotated the framing: the THESIS.md commitment is honoured by the package's *role* as the controller-facing interface, not by its hosting every line of clearance-field code. Same pattern as the audit-before-measure discipline: don't trust the first reading of a commitment without checking what it actually binds.

**Didn't.**

- *Did not move any code.* The decision is structural — Option B leaves the working C++ skeleton in place. The package-boundary ADR is a no-op on disk; the phase-2 kernel work will be a substantive change inside `ceiling_constraint_field/syncCallback` (variance composition, ε(x, y), per-bin δ_cal lookup, operating-point audit) plus net-new code in `hilda_clearance_field/` (spline export module, calibration scaffolding, prototype kernel).
- *Did not write the kernel.* That is the next item — the package boundary was the gate, not the kernel itself. Start by drafting the 02 §Theory equations as a Python/CuPy prototype against the published `variance` layer (against the dual-instance mapping output), then port to a fused C++ CUDA kernel inside the existing `syncCallback`.

**Surfaced.**

- *THESIS.md's package-layout commitments are scope-of-deliverables commitments, not strict file-location commitments.* The "Kept separate from `hilda_ceiling`" wording was right for the controller-facing interface (the thing both controllers genuinely depend on); reading it as also covering the runtime publishing node would have forced a metapackage edit + three sibling retargets for no architectural gain. The audit-before-trust pattern catches the over-binding read. Worth carrying forward: when a commitment is phrased around motivation ("because X will consume from it"), the right unit of analysis is what *consumption* means in-process, not which directory the artefact happens to be authored in.

- *The naming friction is small but worth flagging in code review.* `hilda_clearance_field` now hosts the *interface* to the clearance field rather than its *computation*. The `package.xml` description carries the role; future readers landing in the package first should not have to read 02 §Implementation status to realise the kernel is elsewhere. The README — when one lands — should open with the role split.

- *No further open questions gate phase-2 kernel work.* 02's sim-phase open questions are now: spline knot spacing (decided during phase-3 profiling), provisional λ + ε_base for sim work (a discipline-of-results question, already operational), and the controller-side spline transport (an 05 question, downstream of the spline-export API which lands in this package). None block the variance-aware kernel.

**Changed.**

- `src/thesis/docs/decisions/0010-clearance-field-package-boundary.md` (new) — ADR. Context, three options, choice (B), rationale, consequences with a flip condition tied to G5 measurements.
- `src/thesis/docs/02_variance_aware_clearance.md` §Implementation status — "Package boundary tension" rewritten as "Package boundary settled" with the role split and the ADR link.
- `src/thesis/docs/02_variance_aware_clearance.md` §Open questions / sim-phase — "Package boundary" bullet struck through with *Closed 2026-05-26* annotation pointing at the ADR.
- `src/hilda_clearance_field/package.xml` — `<description>` narrowed to the controller-facing-interface scope; previous text overstated the package's role.
- `src/hilda_clearance_field/setup.py` — `description=` field matched to the narrowed `package.xml` description.
- `src/thesis/journal/2026-W22.md` — this entry.

The boundary call is small but load-bearing: it commits the next two phases (variance-aware kernel + calibration scaffolding) to coherent homes without moving any working code. Phase-2 work — Python/CuPy prototype of the variance-aware kernel first, then port to a fused C++/CUDA kernel inside the existing `syncCallback` — is unblocked.

## 2026-05-26 (late evening continued, prototype kernel start)

**Tried.** First commit on the phase-2 variance-aware kernel prototype, inside `hilda_clearance_field/` per ADR 0010. Scope confirmed before coding: bare kernel + unit tests, no GridMap I/O or runner integration yet (those are subsequent commits).

**Worked.**

- *Two-path module landed.* `hilda_clearance_field/prototype_kernel.py` implements the 02 §Theory formula `c = z_ceil − z_floor`, `σ²_c = max(σ²_zceil + σ²_zfloor, 0)`, `ε = ε_base + δ_cal + λ √σ²_c`, `f = c − h_base − s − ε` in two implementations: `variance_aware_field_reference` (pure array math, works on both NumPy and CuPy via `cp.get_array_module`-style dispatch — formula-of-record, CI-portable without a GPU) and `variance_aware_field_fused` (single CuPy `ElementwiseKernel` writing all four output layers in one pass — performance target the C++/CUDA port will mirror). `FieldParams` dataclass carries `h_base`, `eps_base`, `delta_cal`, `lam` (default 3.0 per 02 §Theory's one-sided coverage at 99.865 %). δ_cal is a scalar — per-bin freshness lookup per ADR 0009 lands when the partner-facility corpus is in hand.
- *NaN propagation + negative-variance clamp behaviour matches between paths.* Both implementations follow IEEE-754 semantics: NaN inputs propagate through arithmetic, the `< 0` comparison against NaN is false so the clamp leaves NaN untouched, and `sqrt(NaN) = NaN` carries through to ε and feasibility. Genuinely-negative variance from numerical noise gets clamped to 0 before the sqrt (defensive — the framework should never publish negatives).
- *Seven-test pytest suite passes in 0.57 s.* Five reference-path tests cover the closed-form cases (zero variance → static-ε, uniform σ² → `λ√(2σ²)`, sign of f at obvious geometries, NaN propagation per cell, negative-variance clamp). Two GPU tests (skipped without CuPy or no device) cover reference-vs-fused parity on 200×200 random grids (atol=1e-6, rtol=1e-6) and the matching NaN-and-clamp behaviour between paths. Parity holding at float32 ulp is the regression discipline ADR 0010 §Consequences names — the prototype is the formula-of-record once the C++ kernel lands.
- *CuPy 13.6.0 + 1 GPU on the workspace, fused kernel built and executes.* No surprises in `ElementwiseKernel` syntax; scalar params are passed as `dtype.type(value)` so they don't carry float64 / float32 mismatch into the elementwise dispatch.

**Didn't.**

- *Did not add GridMap I/O wrappers.* That was option B in the scope question; deferred to the next commit. The natural pairing is to consume `grid_map_msgs/GridMap` via `ros2_numpy` and produce a `GridMap`-shaped output dict so the prototype slots into `compute_ceiling_metrics.py` and the existing experiment runner. Lands when the kernel is integrated against the rosbag corpus.
- *Did not add a microbenchmark.* The 02 §Table 6 0.1 ms field-computation budget is a phase-3 profiling question; the prototype's job at this stage is correctness-of-formula, not throughput.
- *Did not write a separate `docs/superpowers/specs/` design doc.* The brainstorming skill's default workflow would have produced one, but per the thesis disciplines (theory-doc-before-code, doc-in-same-change-as-code) 02 §Theory + ADR 0010 + ADR 0009 are the spec; a duplicate at a non-canonical location would have created drift, not clarity. The skill's HARD-GATE was satisfied by the doc chain already on disk; only the implementation-shape question (module layout, function signatures, test list) was new and got resolved inline before coding.

**Surfaced.**

- *Two-path discipline transfers cleanly from the splitter precedent.* The pattern from ADR 0004 §Consequences (production C++ in `hilda_ceiling/`, Python prototype in `elevation_mapping_cupy/`, both must agree on classifier semantics, config is source of truth) maps directly onto the clearance-field prototype: production C++ in `ceiling_constraint_field` will mirror the reference path's math, the Python prototype in `hilda_clearance_field` is the formula-of-record, parity tests are the cross-check. Worth recording as the working pattern for any future perception-side primitive that needs a sim-phase iteration cycle before the C++ runtime lands.
- *The `cp.ElementwiseKernel` route is the right fused-kernel primitive for cell-wise composite math.* The operation is purely element-wise (no reductions, no shared memory, no warp aggregation) so a `RawKernel` with manual thread organisation would be over-engineering. `ElementwiseKernel`'s compiled-on-first-call behaviour means the first kernel run pays a JIT cost (~hundreds of ms); subsequent runs are at the kernel's compute-bound limit. For the experiment-runner integration this is fine (the kernel runs thousands of times per bag); for any future latency-budget measurement, the JIT cost needs to be warmed off the timing window. Not on the critical path now but worth recording so it doesn't bite later.

**Changed.**

- `src/hilda_clearance_field/hilda_clearance_field/prototype_kernel.py` (new) — reference path + fused kernel + `FieldParams` dataclass + array-module dispatch.
- `src/hilda_clearance_field/test/test_prototype_kernel.py` (new) — 7 pytest cases, 5 NumPy + 2 CuPy-gated. All passing in 0.57 s.
- `src/hilda_clearance_field/setup.py` — `install_requires` gains `numpy`. CuPy stays implicit (system-installed per CLAUDE.md; the package's reference path works without it).
- `src/thesis/journal/2026-W22.md` — this entry.

First substantive code in `hilda_clearance_field/`. Math committed, parity gate in place, NaN/clamp behaviour verified. Next item — GridMap I/O wrapper + integration into `compute_ceiling_metrics.py` so a variance-aware-ε field can be produced on the existing rosbag corpus alongside the static-ε output, for direct comparison.

## 2026-05-26 (late evening continued, compute_ceiling_metrics wire-in)

**Tried.** Pull the prototype kernel into `compute_ceiling_metrics.py` so the existing sim-validation rosbag corpus emits variance-aware-ε stats alongside the elevation/coverage/variance metrics the script already produces. The script subscribes only to the ceiling map (the floor map isn't part of its inputs), so the wire-in lands under the 02 §Calibration protocol ceiling-only working assumption (σ²_zfloor ≪ σ²_zceil) — the kernel evaluates ε from σ²_zceil alone.

**Worked.**

- *Two helpers added to the prototype package.* `epsilon_only(σ², params)` returns the margin as a CuPy/NumPy array given the composed cell variance — convenience for callers that have σ² already and only need ε, not the full clearance/feasibility output. `epsilon_stats(var_values, init_thresh, params)` aggregates ε across a 1D per-cell variance array and returns a JSON-serialisable summary dict mirroring `_compute_variance_stats`'s structure (mean/p50/p90/p95/max/min, post-init subset, init-dominated fraction). Includes a static-ε baseline (`eps_static = ε_base + δ_cal`) and tightening deltas (mean and max of ε − ε_static) so the comparison-vs-static-margin reads off the JSON directly without recomputation downstream.
- *Five additional unit tests, all passing (12/12 total in 0.45 s under sourced overlay).* Closed-form check that ε across a 5-cell synthetic input matches `eps_base + δ_cal + λ √σ²` cell-wise. NaN/negative-variance behaviour for `epsilon_only` matches the kernel's element-wise path. `epsilon_stats` returns None on empty/all-NaN inputs (the gate the script needs). Post-init split fires correctly at init_thresh=50 against a 6-cell input containing 3 post-init + 2 init-dominated + 1 NaN. CuPy↔NumPy parity check at rel=1e-5 across all aggregated stats. The kernel stays the formula-of-record; these new helpers route through it (`epsilon_stats` calls `epsilon_only`, which is the exact `eps_base + δ_cal + λ √max(σ², 0)` form the fused kernel implements).
- *Script wire-in landed.* `elevation_mapping_cupy/scripts/compute_ceiling_metrics.py` now (a) optionally imports the prototype via try/except — graceful degradation to a one-shot warn + null ε output if `hilda_clearance_field` isn't installed, mirroring the existing `_var_layer_missing_warned` pattern; (b) declares three new ROS parameters `eps_base` (default 0.05, matches the static `eps_safety` `constraint_field_node` carries today), `delta_cal` (default 0.0 per 02 §Open questions / sim-phase), `lam` (default 3.0 per 02 §Theory's 99.865 % one-sided coverage point); (c) computes `result['epsilon']` and `result['epsilon_core']` adjacent to the existing `variance`/`variance_core` blocks, gated on the same `live_var is not None` precondition; (d) extends the per-sample log line with `ε post-init=<μ> (static=<s>, +<δ> avg)`; (e) extends the JSON output with top-level `epsilon`/`epsilon_core` plus per-sample entries, and a `params.epsilon_assumption` string that records the ceiling-only working assumption so a future reader of the JSON knows the inputs without consulting the script.
- *Build clean.* `colcon build --packages-select hilda_clearance_field --symlink-install` finished in 0.96 s. Post-build import check confirms the script's optional-import branch resolves `_HAS_PROTOTYPE = True` under the sourced overlay; a hand-crafted call to `epsilon_stats` with a 3-cell input reproduced the closed-form ε mean to 4 d.p. of the analytic value.

**Didn't.**

- *Did not also extend the diagnostic plot.* The script's 4-panel plot is a separate add — JSON is the canonical surface downstream postprocess scripts (01b's `sim_validation_01_*_postprocess.py`, 01d's analogue) consume, and the ε stats land there cleanly. The plot extension would add a 5th panel for ε(x,y) spatially; defer until either a postprocess script wants it or a reviewer asks for the figure.
- *Did not run the wire-in end-to-end against a rosbag.* That's the experiment-runner integration step, and requires a config file under `thesis/experiments/configs/` plus an entry in `_LABEL_ENCODERS` for the new ε parameters if the runner needs to sweep them. The wire-in is unit-tested at the prototype-side (12 passing) and import-tested at the script side; the end-to-end runs land as part of the next experiment (a variance-aware-ε corpus sweep against the existing bag, naturally tagged as `sim_validation_02a` or similar).
- *Did not subscribe to the floor map.* Adding `message_filters` over both topics restructures the script's callback flow; under the 02 §Calibration protocol ceiling-only working assumption the floor contribution is absorbed into the calibration residual, so the current single-subscriber form is correct for this stage. The composed-margin extension (σ²_zfloor + σ²_zceil) lands when the partner-facility corpus is collected and the assumption is verified or rejected per the 02 doc's verification step.

**Surfaced.**

- *The optional-import + one-shot-warn pattern is the right interface between a vendored upstream and a thesis-stage package.* `elevation_mapping_cupy` is a vendored fork (CLAUDE.md flags it explicitly) — adding a hard `<depend>hilda_clearance_field</depend>` to its `package.xml` would carry into every future upstream pull and make merges painful (ADR 0004 §Rationale's maintenance-posture argument applies here too). Try/except plus a `_HAS_PROTOTYPE` flag plus a startup warn gets the new functionality available when the workspace is built end-to-end without coupling the upstream's manifest to the thesis-stage package. Same as ADR 0004 §Consequences for the splitter prototype — the pattern generalises whenever a sim-phase tool wants to consume a thesis-stage primitive from inside vendored code.
- *`epsilon_stats` returning JSON-friendly plain Python floats (vs `np.float32` or `cp.ndarray`) avoids a serialisation footgun.* `_compute_variance_stats` already does this implicitly via `float(np.mean(...))`; the new helper does it explicitly via a small `_tofloat` that handles the CuPy 0-d-array case too. A naive implementation would write `cp.ndarray(0.0268, dtype=float32)` into the dict; `json.dumps` would raise `TypeError`. Worth recording: any sim-phase tool consuming the prototype's outputs into JSON needs the `_tofloat` hop. Codifying this in the prototype keeps the downstream surface clean.
- *The static-baseline-plus-tightening-deltas reporting shape pre-empts a class of downstream questions.* The dict carries `eps_static`, `tightening_mean`, `tightening_max` alongside the raw ε stats. A reviewer or supervisor asking "how much does the variance-aware ε actually tighten the margin vs the static-safety baseline" reads off the JSON in one line; without those fields they'd have to derive the static baseline from the parameter block by hand or trust the script's log line. Small upfront thought, large downstream conversation-cost reduction. Generalise: any sim-phase metric whose interpretability rests on a delta-vs-baseline comparison should carry the baseline next to the measurement, not in a separate block the reader has to assemble.

**Changed.**

- `src/hilda_clearance_field/hilda_clearance_field/prototype_kernel.py` — added `epsilon_only(σ², params)` and `epsilon_stats(var_values, init_thresh, params)`. Both route through the same `eps_base + δ_cal + λ √max(σ², 0)` form the kernel implements; `epsilon_stats` returns JSON-serialisable plain Python types.
- `src/hilda_clearance_field/test/test_prototype_kernel.py` — five new tests covering `epsilon_only` (closed-form + NaN/clamp) and `epsilon_stats` (closed-form + post-init split + empty-input None + CuPy/NumPy parity). 12 tests total, all pass in 0.45 s under the sourced overlay.
- `src/elevation_mapping_cupy/elevation_mapping_cupy/scripts/compute_ceiling_metrics.py` — optional prototype import at top of module, three new ROS parameters (`eps_base`, `delta_cal`, `lam`), ε computation in `_compute_metrics` adjacent to the variance block, log-line extension, JSON top-level + per-sample blocks, `params.epsilon_assumption` text recording the ceiling-only working assumption.
- `src/thesis/journal/2026-W22.md` — this entry.

The prototype is now exercisable against the existing sim-validation rosbag corpus by re-running any of the 01/01b/01c/01d configs through the experiment runner — ε stats will land in the same `summary.json` the variance stats already do. Next item: a small `sim_validation_02a` config that runs a δ_cal-and-λ sweep against the same persistent bag, producing the structural-behaviour numbers 02 expects (mean ε at static vs variance-aware, fraction of cells where ε exceeds the static baseline by more than 1 cm, sensitivity of post-init mean ε to λ). That experiment is the first deliberate exercise of the prototype against thesis data and the natural anchor for the next ADR if the regime turns up anything unexpected.

## 2026-05-26 (late evening continued, review-driven fixes + 02a design)

**Tried.** Two streams: (a) launch a code-reviewer agent against the session's diff, apply the receiving-code-review discipline — verify each finding before implementing, push back with technical reasoning on wrong ones — and land confirmed fixes; (b) design `sim_validation_02a` as the first deliberate exercise of the prototype against the persistent rosbag, with falsifiable predictions and acceptance gates.

**Worked.**

- *Code-reviewer agent surfaced four substantive findings, all verified against the code before implementing.*
  - **Bug 1.** `_prototype_missing_warned = False` initialised in `compute_ceiling_metrics.py` but never read or set to `True` anywhere. The per-call guard was planned and forgotten; the startup warn against the module-level `_HAS_PROTOTYPE` constant is sufficient. Fix: delete the dead field. Confidence-of-finding: 100 %.
  - **Bug 2.** `epsilon_only(sigma2, params)` crashes on Python-scalar input with `AttributeError: 'float' object has no attribute 'dtype'`. The function is exported as public, docstring said "works on NumPy or CuPy" without restricting to arrays. The internal call path through `epsilon_stats` is array-only (slices), so the bug wasn't exercised by the existing tests; a REPL sanity-check or a future sweep config reader would hit it. Fix: `xp.asarray(sigma2)` before the clamp; new test asserts the scalar-input path returns a 0-d array of the right value. Confidence-of-finding: verified.
  - **Issue 3.** `tightening_mean` and `tightening_max` are population-level (all finite cells, including init-dominated where σ² ≈ 100 and ε ≈ 30); a caller reading them as "tightening on well-measured surfaces" gets the wrong number when any cells remain init-dominated. Fix: added `tightening_post_init_mean` and `tightening_post_init_max` keys parallel to the existing post-init subset stats; docstring names the load-bearing comparison explicitly. Test covers the closed-form value on a synthetic 3-post-init + 2-init-dominated split. Confidence-of-finding: verified.
  - **Trade-off 4.** ADR 0010 §Consequences flip-condition wording understated the retargeting cost: if `/constraint_field` becomes debug-only the three siblings (`ceiling_height_lookahead`, `ceiling_collision_monitor`, `ceiling_controller`) need retargeting too, i.e. the cost Option B *defers* rather than eliminates. One-sentence addition to the ADR records this honestly.
  - The reviewer self-retracted finding #2 (`_tofloat` on NumPy scalars works correctly — `float(np.float32(x))` is valid) and confirmed #5 (`dtype.type(params.h_base)` for ElementwiseKernel scalars is the right form, not a bug). The CuPy 13.6.0 install on this workspace carries `cp.median` so trade-off #7 (CuPy version dependency) doesn't bite; flagged for portability if the workspace ever downgrades.

- *13/13 tests green after fixes (was 12/12 before).* New tests: `test_epsilon_only_accepts_python_scalar` + extended `test_epsilon_stats_closed_form_and_post_init_split` to assert the new post-init tightening fields satisfy `tightening_post_init_mean < tightening_mean` whenever any cells are init-dominated (the actual case the new keys are needed for). All pass in 0.47 s.

- *`sim_validation_02a` designed and committed.* Config: `experiments/configs/sim_validation_02a/variance_aware_epsilon_baseline.yaml` — single-cell matrix at full production (cs=0.10, snf=0.05, es=true, wnt=20, tv=0.0001), identical to 01d's tv=0.0001 cell so reproducibility hinges on sub-mm agreement on elevation RMSE and sub-percent on σ² post-init mean. Metric-script ε parameters stay at canonical defaults (λ=3, δ_cal=0, ε_base=0.05) — no runner extension needed for 02a; the λ-and-δ_cal sweep follow-up (02b) will pass-through metric-script params from the YAML when it lands. Postprocess script `runners/sim_validation_02a_postprocess.py` produces five verdict blocks: reproducibility, ε quantile cross-check, Jensen bound, tightening baseline, consistency.

- *The ε quantile cross-check is the integration-correctness gate.* Because √ is monotonic non-decreasing on σ² ≥ 0, the kernel preserves quantiles: `ε_p50 − ε_static = λ √σ²_p50`, same for p90 and p95, on both the full and post-init populations. Five paired checks, 1 mm tolerance — verifies the script's wire-in path translates published σ² to ε faithfully end-to-end, not just at the unit-test level. A failure indicates either the metric-script's parameter reads, subset selection, or call-site dispatch is off; the kernel itself is already unit-tested. The postprocess smoke-test against a hand-constructed synthetic summary.json reproduces all five PASS verdicts and renders the headline table correctly.

- *The Jensen bound is the structural sanity gate.* By Jensen on √ (concave), `tightening_post_init_mean ≤ λ √σ²_post_init_mean`. The gap (ceiling − measured) reflects the within-bag σ² distribution shape; the slack is 5 mm. A violation indicates either the kernel mis-aggregates or the bag's σ² distribution has unusual numerical properties. The synthetic smoke-test produces a +24.2 mm gap (5 % below the Jensen ceiling, consistent with a moderately-skewed σ² distribution).

- *The tightening baseline is the headline number the thesis cites.* `tightening_post_init_mean` reports the average extra margin the variance-aware kernel demands on well-measured surfaces, over the static ε_safety = 50 mm baseline `constraint_field_node` currently carries. At production σ² ≈ 0.026 m² and λ=3 the Jensen ceiling is ≈ 0.484 m; the in-range gate [20, 500] mm is generous on the upper bound for the synthetic case where σ² distribution shape isn't yet measured, tightens once 02a runs on the real bag.

**Didn't.**

- *Did not launch the 02a experiment.* Following the established pattern — Oscar launches the experiment himself, same as 01c / 01d. The config + postprocess are ready; runner expects the standard invocation; defaults match. ~9 min wall on the persistent bag.

- *Did not extend the experiment runner to pass-through metric-script parameters from the YAML.* Deferred to 02b. For 02a the script's ROS-param defaults match the canonical values (λ=3, δ_cal=0, ε_base=0.05), so no pass-through is needed. When 02b designs a λ-sweep or δ_cal-sweep, the runner gains a `metrics.extra_params: {lam: ..., delta_cal: ...}` block that gets propagated to the metric-script's `--ros-args` list. Small change, ~5 lines.

- *Did not also restructure 02 §Open questions / sim-phase to reflect the queued 02a experiment.* The "Provisional λ and ε_base for sim work" bullet in 02 §Open questions already covers the discipline (results manifests carry the "δ_cal pending hardware calibration" annotation); 02a's config honours it via the `params.epsilon_assumption` field in the JSON output. No doc edit needed — the open question is correctly framed and stays open until the hardware calibration corpus arrives.

**Surfaced.**

- *Audit-before-trust on agent outputs is now a working pattern with two passes.* This session ran two review agents (the morning's diagnostic-resolution agent that produced ADR 0009, and this evening's code-reviewer agent over the session's diff). Both produced substantive findings that the original author wouldn't have surfaced alone. Both required spot-verification — the morning's against the framework code, this evening's against the per-finding code paths. Both surface findings that were structurally correct + needed implementation. The pattern is: spawn → spot-verify → apply with technical acknowledgment, never blind implementation. The receiving-code-review skill calls out the exact same protocol; the new entry is that the same audit-before-trust applies to spawned agents regardless of role (diagnostic vs review).

- *The integration-test gate at the script-wire-in boundary is qualitatively different from the kernel-side unit test gate.* The unit tests verify the kernel's per-cell math against synthetic inputs; the quantile cross-check in 02a verifies that the script's wire-in (parameter reads, subset selection, call site, JSON serialisation) correctly invokes the kernel on real-bag inputs. Both gates are needed; neither subsumes the other. Generalise: any sim-phase tool that wires a thesis-stage primitive into vendored upstream code needs (a) unit tests on the primitive, (b) a structural integration check that the wire-in invokes the primitive faithfully. 02a's quantile cross-check is the template — a closed-form identity that must hold end-to-end through the wire-in, with tight tolerance, on a real bag.

- *The headline-number-plus-acceptance-band design is what makes single-run experiments falsifiable.* 02a is a single run with no sweep matrix — without falsifiable predictions it would be exploratory, not validation. The five verdict blocks (reproducibility, quantile cross-check, Jensen bound, tightening baseline, consistency) are quantitative gates that catch four distinct failure modes (upstream drift, wire-in defect, kernel aggregation bug, parameter-default silent failure). Any single-run experiment in a validation series wants this shape: one headline number with a calibrated acceptance band, plus a clutch of structural checks that catch the silent-failure cases. The series-wide pattern from 01b/01c/01d (reproducibility + sweep-specific gates + sanity bounds) generalises to non-sweeping experiments via this construction.

**Changed.**

- `src/elevation_mapping_cupy/elevation_mapping_cupy/scripts/compute_ceiling_metrics.py` — removed dead `_prototype_missing_warned` field per reviewer bug 1.
- `src/hilda_clearance_field/hilda_clearance_field/prototype_kernel.py` — `epsilon_only` now accepts Python-scalar input via `xp.asarray` (reviewer bug 3); `epsilon_stats` exposes `tightening_post_init_mean` + `tightening_post_init_max` parallel to the existing population-level numbers (reviewer issue 4); docstring names which is load-bearing.
- `src/hilda_clearance_field/test/test_prototype_kernel.py` — new `test_epsilon_only_accepts_python_scalar`; extended `test_epsilon_stats_closed_form_and_post_init_split` to verify the new post-init tightening fields. 13 tests total, all pass in 0.47 s under the sourced overlay.
- `src/thesis/docs/decisions/0010-clearance-field-package-boundary.md` — flip-condition wording now states explicitly that the retargeting cost Option B defers is not eliminated under the flip (reviewer trade-off 6).
- `src/thesis/experiments/configs/sim_validation_02a/variance_aware_epsilon_baseline.yaml` (new) — single-cell production-geometry config with five-block acceptance gates (reproducibility, ε quantile cross-check, Jensen bound, tightening sanity, consistency).
- `src/thesis/experiments/configs/sim_validation_02a/README.md` (new) — falsifiable predictions, falsification cases, operating point, read-out, cost (~9 min wall), explicit scope-of-not.
- `src/thesis/experiments/runners/sim_validation_02a_postprocess.py` (new) — five verdict blocks, structural-vs-reproducibility exit-code split (return 2 for structural fail, 3 for reproducibility fail, 0 for PASS). Smoke-tested against a synthetic summary.json — all five blocks render correctly, math checks out by construction.
- `src/thesis/journal/2026-W22.md` — this entry.

The review pass + the queued 02a together close the prototype's sim-phase entry into the experimental loop: kernel is correct (unit tests), wired in correctly (12+ new tests, review-driven fixes for the three confirmed defects), and ready to produce its first quantitative read on the production bag with falsifiable acceptance gates. Next: launch 02a (~9 min wall, no human work beyond the launch command); read the headline `tightening_post_init_mean`; decide whether to queue 02b (λ-sweep with runner extension) immediately or to move to phase-2 work on the C++/CUDA runtime kernel in `ceiling_constraint_field`. The decision rests on whether the 02a baseline numbers point at any unexpected structural behaviour worth investigating before the C++ port commits.

## 2026-05-26 (late evening continued, 02a first launch + STRUCTURAL FAIL diagnostic + cadence-conditional empirical finding)

**Tried.** Launch 02a (~9 min wall). Postprocess exit code 2 (STRUCTURAL CHECK FAILED). Switched to root-cause investigation per the systematic-debugging discipline before proposing fixes — read sweep_summary.md, summary.json, metrics_diagnostic.json side-by-side; compared the 02a artefacts against the archived 01d/01c counterparts.

**Worked.**

- *Phase 1 root cause: three distinct issues, isolated by reading the data.*
  - *Issue R-1 (runner-side bug).* The metric script's `metrics_diagnostic.json` IS produced correctly — `epsilon`, `epsilon_core`, `params.epsilon_assumption`, `params.prototype_available: True` all present and populated with kernel-correct values. The bug is downstream: the runner's `_run_single_cell` projection step at `sim_validation_01_joint_sweep.py:347-349` reads `m.get("variance")` + `m.get("variance_core")` from the script's JSON into the per-cell `summary.json`, but does **not** read `m.get("epsilon")` + `m.get("epsilon_core")`. The runner's `summary.json` is what the postprocess consumes, so the postprocess saw "ε block missing from summary.json" and failed. Two-line fix lands the ε pass-through into the runner. Independent of the wire-in; nothing wrong with the kernel or the script.
  - *Issue R-2 (postprocess-side bug).* `sim_validation_02a_postprocess.py::reproducibility_block` read `summary.get("all").get("rmse")` and `summary.get("core").get("rmse")`. But the runner's `summary.json` carries RMSE flat at top-level (`RMSE_all`, `RMSE_core`), not nested under `all`/`core` — that nested structure is the metric script's `metrics_diagnostic.json` layout, which the runner re-projects flat. Result: both reads silently returned None, the comparison silently passed at "+0.0 mm" against the archived "+0.0 mm". After the fix, the real numbers surface: current RMSE_all = 0.3075 m vs archived 0.3025 m (Δ = +5.0 mm); RMSE_core = 0.1956 vs 0.1998 (Δ = −4.2 mm). Both essentially at the ±5 mm tolerance, not the silent "+0.0" the bug had been reporting. Important lesson: a "PASS" verdict with structurally-impossible numbers is a silent failure mode — the bug-from-the-start was the comparison was never running, not that the numbers were too good.
  - *Issue R-3 (substantive — splitter throughput regression).* The σ² post-init mean in 02a is 0.101 m² vs 01d archived 0.027 m² (3.78× ratio, 277.6 % relative Δ — way outside the ±5 % reproducibility tolerance). Same bag (file mtime + size unchanged), same params (rendered configs byte-identical), same code path through the elevation node. Inspection of the per-cell timing CSVs and `drops_*.yaml`: 02a's ceiling elevation node processed 298 PointCloud2 messages vs 01d's 4929 — a **16× reduction in input-callback rate over the same 514 s bag replay**. Publishing rate (timing_*_publish.csv) is identical at 7675 / 7705 — the elevation node's GridMap publish-timer fires on the same schedule, but the underlying state only updates 298 times vs 4929. The point clouds are getting dropped upstream of the elevation node — at the splitter, or at the bag-to-splitter pipeline. Bag has 5147 native PointCloud2 messages at ~10 Hz; 02a delivered 5.8 %, 01d delivered 95.7 %. Likely transient GPU/CPU contention slowing the splitter, causing TF-stale drops at its 0.2 s timeout gate.

- *The σ² mismatch in (R-3) has the SAME root cause as the throughput drop, and the relationship is a load-bearing prediction from ADR 0009 §F2.* The √-floor expression `σ² ≈ √(α_d · z² · time_variance · update_variance_fps · Δt)` has Δt as the per-cell inter-observation cadence. 16× fewer input frames → 16× larger Δt → predicted σ² ratio = √16 = 4.0×. Measured ratio: **3.77×.** Agreement within 7 %. The cadence range the unplanned regression exercised (16×) is wider than any deliberately-designed experiment would have produced; the fact that the √-floor's response shape holds across it is a **substantive empirical confirmation** of ADR 0009 §F2's mechanism — beyond the planned 01d sweep which only covered the `time_variance` axis with cadence held fixed. The published σ²'s *absolute* level at production is still set by the outlier-injection branch (01d §Zero-inflation FAIL); the *response shape* under cadence change is the √-floor. Both mechanisms now have independent empirical confirmation. Memory entry [[variance-layer-regime]] updated to record this.

- *The 5 verdict blocks after fixing R-1 and R-2 (hand-patched the existing summary.json with the ε block from `metrics_diagnostic.json` to verify the diagnosis without re-running):*
  - ε quantile cross-check: **PASS** — all 5 quantiles (p50/p90/p95 on full, p50/p90 on post-init) match `eps_static + λ √σ²_q` to ±0.00 mm at 1 mm tolerance.
  - Jensen bound: **PASS** — tightening_post_init_mean = 836 mm, λ √σ²_post_init_mean = 954 mm, gap = +118 mm (≫ 5 mm slack).
  - Consistency: **PASS** — ε.init_dom = var.init_dom = 0.00 % (within 0.5 % tol).
  - Reproducibility: **FAIL** — RMSE within tol (after R-2 fix), σ² 277.6 % off — driven by R-3.
  - Tightening baseline: **FAIL** — 836 mm > 500 mm upper band — also driven by R-3 (σ² is high → λ √σ² is high → tightening is high).
  - **The kernel + wire-in are structurally correct (3/3 structural PASSes). Both FAILs are downstream of R-3, the throughput regression.**

- *Re-run launched in background (shell `bf1z1c3tz`, ~9 min) with the runner fix in effect.* If splitter throughput recovers (~10 Hz processing rate as in 01d), σ² will return to ~0.027 m² and tightening_post_init_mean to ~250–480 mm, which sits inside the [20, 500] mm acceptance band — a clean 5/5 PASS. If throughput stays low, the new regime IS the new baseline and the band needs recalibration. Either outcome is informative.

**Didn't.**

- *Did not investigate the splitter's drop counters directly.* The splitter's drop-attribution would be in its own log (TF-stale vs queue-overflow vs other), but the runner doesn't currently persist the splitter's log file. Could be added (small runner change) — deferred until/unless the throughput regression repeats. For now the working hypothesis is transient GPU/CPU contention, the cheapest test is just re-running.
- *Did not loosen the tightening acceptance band.* The "FAIL" verdict at 836 mm > 500 mm is doing real work — it's the gate that surfaced the regime change. Widening it to fit the data would hide the very signal it's catching. The band stays at [20, 500] mm; if the throughput-recovery re-run pushes σ² back to the 01d regime the gate will pass without intervention.
- *Did not re-build elevation_mapping_cupy after the morning's `compute_ceiling_metrics.py` edits.* Verified via `params.prototype_available: True` in metrics_diagnostic.json + the `epsilon_assumption` text being present — the installed script IS the updated one. Build state is correct.

**Surfaced.**

- *A silent failing PASS is worse than a vocal FAIL.* The postprocess's "RMSE_all Δ = +0.0 mm PASS" was structurally impossible (the bag changes between runs at the millimetre level — exact +0 means the comparison didn't run) but the verdict line said PASS. Only the σ² FAIL surfaced because variance.post_init_mean was the only field whose path was correct. The lesson: any "compared at zero difference" outcome should be inspected for "compared at all," not assumed correct. Two cross-checks that would have caught this earlier: (a) refuse to print "PASS" when the inputs are None; (b) print "Δ = N/A" not "Δ = +0.0 mm" when one side is missing. Worth a follow-up to the existing postprocess scripts (01b/01c/01d may have the same latent pattern in their reproducibility readers — verify when next visited).

- *The unplanned 16× cadence regression is a more decisive empirical test of ADR 0009 §F2 than 01d itself.* 01d swept `time_variance` ∈ {0, 1e-4, 1e-3, 1e-2} at fixed cadence; the √-scaling pairwise PASSes were over a 100× range in `time_variance` (predicting √100 = 10× in σ², measured 7.6× — within ±50 % tolerance). The 02a-vs-01d-archived comparison sweeps cadence at fixed `time_variance` over a 16× range; predicting √16 = 4.0× in σ², measured 3.77× — agreement to 7 %. Cadence is the harder axis to control deliberately (it depends on bag content + system load), but the unplanned exercise of it actually produced a tighter empirical confirmation. Worth recording as a working pattern: when an experiment fails for reasons orthogonal to its planned axis, the orthogonal variation may itself be a substantive measurement worth keeping. Don't reflexively discard "noise" — characterise it.

- *The runner / postprocess interface is the systematic-debugging hot spot for sim-phase experiments.* This turn surfaced **two** bugs in that exact interface (runner doesn't propagate new metric fields, postprocess reads from a different schema than the runner writes). Both are quiet defects — the experiment "runs" successfully and produces output; the bugs manifest only when the postprocess actually consumes the data. Generalise: whenever a new metric is added to `compute_ceiling_metrics.py`, three things need to land together — (i) the script's `_save_metrics_json` writes it (current pattern: nested under appropriate key in `metrics_diagnostic.json`); (ii) the runner's `_run_single_cell` projects it into `summary.json` (current bug: this step was missed for ε); (iii) the postprocess reads from the **runner's** flat layout, not the script's nested layout (current bug: this was wrong for RMSE in the new 02a postprocess). Drafting a checklist for future metric-additions would prevent the next instance of this class of bug. Defer the checklist itself; surface here as a pattern.

**Changed.**

- `src/thesis/experiments/runners/sim_validation_01_joint_sweep.py` — `_run_single_cell` projection step now reads `m.get("epsilon")` and `m.get("epsilon_core")` from the metric script's JSON into the per-cell summary (4-line addition).
- `src/thesis/experiments/runners/sim_validation_02a_postprocess.py::reproducibility_block` — RMSE field reads now use the runner's flat `RMSE_all` / `RMSE_core` top-level keys, not the nested `all.rmse` / `core.rmse` that don't exist in the runner's projection. The bug had been silently producing "+0.0 mm" PASSes against missing data.
- `src/thesis/experiments/results/sim_validation_02a/variance_aware_epsilon_baseline__5abb8400ad08_d34c74e/cs010_es1_snf050_tv00100_wnt020/summary.json` — hand-patched to inject the ε block from `metrics_diagnostic.json`, so the postprocess could verify the structural gates against this run's data without re-running the experiment. (The background re-run produces a clean new result dir with the runner fix in effect; this hand-patched dir stays as the artefact-of-record for the diagnostic.)
- `~/.claude/projects/.../memory/feedback_variance_layer_regime.md` — added the 16× cadence empirical confirmation of σ² ∝ √Δt (predicted 4.0×, measured 3.77×, agreement within 7 %); records the consequence for δ_cal that the deployment observation cadence regime must be locked alongside the four framework parameters at calibration time.
- `src/thesis/journal/2026-W22.md` — this entry.

The diagnostic arc closes with two bugfixes (runner ε pass-through, postprocess RMSE field path), a structural verification that the wire-in is correct (3/3 structural PASSes on today's data), and an unplanned empirical confirmation of ADR 0009 §F2's √-floor mechanism at 16× cadence variation. The background re-run, in flight at journal commit time, tests whether the throughput regression was transient or persistent — outcome lands in the next sub-entry.

## 2026-05-26 (late evening continued, 02a re-run PASS — first variance-aware ε baseline on production data)

**Tried.** Re-run 02a with the two bugfixes in effect (runner ε pass-through + postprocess RMSE field path), test whether the splitter-throughput regression was transient.

**Worked.**

- *Throughput recovered cleanly.* 4942 ceiling callbacks + 4887 floor callbacks over the same 514 s bag — back in 01d's regime (4929 / 4885). Splitter delivered 96 % of the bag's 5147 native PointCloud2 messages, vs 5.8 % on the failed run. Confirms the previous regression was transient GPU/CPU contention, not a persistent pipeline issue.
- *5/5 PASS on the postprocess.* Reproducibility: RMSE_all Δ = +0.1 mm, RMSE_core Δ = +0.1 mm (essentially identical to archived 01d), σ² post-init mean Δ = +1.4 % (current 0.02715 vs archived 0.02679, well inside the ±5 % tolerance). ε quantile cross-check: all 5 quantiles match `ε_static + λ √σ²_q` to ±0.00 mm at 1 mm tolerance. Jensen bound: tightening_post_init_mean = 424.2 mm, ceiling λ √σ²_post_init_mean = 494.3 mm, gap = +70.2 mm (well above the 5 mm slack — consistent with a moderately right-skewed σ² distribution at production cadence). Tightening baseline: 424.2 mm sits inside [20, 500] mm. Consistency: ε.init_dom = var.init_dom = 0.00 %.
- *The headline thesis number lands.* **tightening_post_init_mean = 424.2 mm** is the first quantitative answer to "how much does variance-aware ε tighten the static safety margin on well-measured surfaces in production conditions" — about 8.5× the static ε_safety = 50 mm baseline `constraint_field_node` currently carries. At λ = 3 the chance constraint asks for 99.865 % one-sided coverage of the dangerous tail; the per-cell σ² distribution at production cadence produces that 424 mm average tightening as the price of the coverage guarantee. δ_cal is still 0 (sim phase, no calibration corpus yet); the final deployment number will be 424 mm + δ_cal, where δ_cal accounts for the residual gap between the framework's √-compressed σ² and the empirical residual on the partner facility.
- *tightening_post_init_max = 3043 mm* (the worst-case cell). That cell carries σ²_max ≈ 1.03 m² (back-solved from 3.043 m = 3 √σ²_max), consistent with the variance.max = 1.08 the variance block reports — likely an FOV-edge or outlier-injection-dominated cell. The constraint field would refuse to plan through that cell at production margins; the planner's soft-constraint slack policy decides whether that's acceptable (03's question).
- *The ε quantile cross-check verifies the wire-in at production data, not just synthetic.* The 5/5 ±0.00 mm match is end-to-end through the script's parameter reads, the prototype's `epsilon_stats` aggregation, the runner's projection into summary.json (after the bugfix), and the postprocess's closed-form check. Same integration-gate confidence the synthetic smoke-test produced, now against real-bag data.

**Didn't.**

- *Did not re-archive the failed-run artefacts.* The deterministic-naming property of the runner (config_sha + git_sha → result dir name) means the re-run wrote to the same `__5abb8400ad08_d34c74e/` directory and overwrote the failed-run outputs. The previous entry's claim that the hand-patched summary "stays as the artefact-of-record for the diagnostic" was wrong — it was overwritten. The diagnostic narrative survives only in this journal + in [[variance-layer-regime]]. Acceptable in practice (the journal IS the audit trail for the diagnostic), but worth flagging: when a re-run produces a clean PASS over a previously-failed run, the failed-run on-disk evidence is lost. For future diagnostics on a failed run, copy the relevant artefacts to a `__<hash>_failed/` sidecar dir before re-running, so the comparison remains rerunnable. Defer the runner change to support this; it didn't bite here because the journal's quantitative description is rich enough to reconstruct the diagnostic.
- *Did not investigate the failed-run's splitter logs.* The earlier hypothesis (transient GPU/CPU contention causing splitter slowness → TF-stale drops) is consistent with the clean recovery on the re-run, but the direct evidence (per-frame splitter drop attribution) wasn't captured. The runner doesn't persist the splitter's log file. If the regression recurs, persist the splitter logs first.

**Surfaced.**

- *The 5/5 PASS gives the first thesis-citable number for variance-aware ε in production conditions: tightening_post_init_mean = 424 mm at λ = 3, ε_base = 50 mm, δ_cal = 0.* The thesis can now claim, with the wire-in verified end-to-end and reproducibility against archived data, that the variance-aware kernel demands an average 424 mm extra margin on well-measured ceiling surfaces compared to the static 50 mm baseline. This is the headline result Module 02 was scoping toward; subsequent experiments refine it (per-cadence δ_cal, partner-facility calibration corpus, λ-sweep sensitivity) but the load-bearing first number is on disk.
- *The deterministic-naming property of the runner is a footgun for failed-run forensics.* Two consecutive runs with the same config + same git state overwrite each other on disk. The runner's CSV/MD summary accumulates rows for matrix-multi-cell sweeps, but for single-cell configs (like 02a) the re-run wipes the prior measurement. Worth recording as a working pattern: any single-cell experiment that fails should have its result dir copied to a sidecar `__<hash>_failed/` before re-running, OR the runner should be extended to detect existing populated dirs and prompt-or-archive instead of overwriting. The journal narrative + memory entries are the audit trail of last resort, but those don't capture per-cell summary.json / metrics_diagnostic.json artefacts.
- *The 02a result + the 02a-failed result together produce two independent confirmations of ADR 0009.* The failed run validates the √-floor's *cadence response* (σ² ∝ √Δt at 16× cadence variation, agreement within 7 %); the PASS run validates the production-cadence absolute value (σ² post-init mean reproduces 01d to 1.4 %). Two distinct properties of the same mechanism, two distinct empirical tests, both PASSing. The mechanism's predictive value for δ_cal calibration is now strongly supported by independent data points. If the partner-facility calibration produces residuals consistent with this regime, the chance-constraint identity in 02 §Theory ports cleanly.

**Changed.**

- `src/thesis/experiments/results/sim_validation_02a/variance_aware_epsilon_baseline__5abb8400ad08_d34c74e/` — entire result dir rewritten by the re-run; contains the clean 5/5 PASS sweep_summary.md plus the per-cell summary.json with the populated ε block (runner pass-through working end-to-end). Headline number: `epsilon.tightening_post_init_mean = 0.4242 m` in the per-cell summary.json.
- `src/thesis/journal/2026-W22.md` — this entry.

The first variance-aware ε baseline on production data lands cleanly: 424 mm tightening on well-measured surfaces, with structural verification (5 quantiles to float precision), reproducibility against archived 01d (sub-mm on elevation, 1.4 % on σ²), and Jensen-bound sanity (70 mm gap below the ceiling). The kernel + wire-in are sim-phase complete. Next: either 02b (λ-sweep with runner extension to surface the sensitivity-to-λ figure 02's §Theory predicts), or move phase-2 work to the C++/CUDA port inside `ceiling_constraint_field/syncCallback`. The 02b path produces one more thesis figure (the linearity of tightening_post_init_mean in λ, which the quantile cross-check structurally verifies but does not visualise); the C++ port produces the production-runtime artefact that the controller will actually consume. Both are well-scoped at this point.

**End of 2026-05-26.** Day delivered ADR 0010 (package boundary), the Python/CuPy prototype kernel with 13 passing tests, the `compute_ceiling_metrics.py` wire-in, the 02a baseline (`tightening_post_init_mean = 424 mm` at production cadence, reproducible against archived 01d), an unplanned empirical confirmation of ADR 0009 §F2 at 16× cadence variation (incorporated into [[variance-layer-regime]]), and three review-driven plus two diagnostic-driven bugfixes across the runner, postprocess, and kernel. Module 02's sim-phase arc is closed; phase-2 (variance-aware C++/CUDA in `ceiling_constraint_field`) and 02b (λ-sweep with runner extension) are the two open paths into the next session.

## 2026-05-26 (late evening continued, 02b runner extension + λ-sweep scaffold)

**Tried.** Carry the day's fork forward: phase-2 C++/CUDA port versus 02b λ-sweep. Picked 02b — the cheaper sim-phase deliverable closes 02's sim-phase characterisation arc with one more thesis figure (tightening vs λ at fixed framework parameters), the runner extension is bounded (~5 lines as the earlier journal predicted), and the C++ port can take the prototype's reference path as the formula-of-record without retroactive changes. Scope of the session: runner extension + 02b config + 02b README + 02b postprocess + dry-run + smoke-test + 02 §Implementation status update, single coherent change set. No experiment launch — that follows the user-launches-the-bag pattern from 01c / 01d / 02a.

**Worked.**

- *Theory check before code.* 02 §Theory's prediction at fixed framework parameters is structurally exact, not approximate: σ² is λ-independent at fixed `time_variance` + `sensor_noise_factor` + `update_variance_fps` + same bag, so `tightening_post_init_mean(λ) = E[ε(λ) − ε_static] = λ · E[√σ²_post_init]` with E[√σ²_post_init] invariant across cells. No Jensen gap — the Jensen factor is between `E[√σ²]` and `√E[σ²]`, but the tightening is on `E[λ √σ²] = λ E[√σ²]` directly. The cross-cell falsifiability gate is therefore slope invariance, not a Jensen-bounded inequality: `tightening_post_init_mean / λ` must be invariant across cells to within σ²-reproducibility tolerance (~1.4 % per the 02a-vs-archived-01d measurement, ±5 % gate is generous). This is the headline structural gate the 02b postprocess fires.

- *Runner extension landed in three minimal edits.* Added `METRICS_PARAM_KEYS = {"lam", "delta_cal", "eps_base"}` constant alongside the existing `NON_ELEVATION_KEYS` skip-set in `sim_validation_01_joint_sweep.py`; extended `_LABEL_ENCODERS` with `lam`/`delta_cal`/`eps_base` encoders (×10 / ×1000 / ×1000 padded to keep the per-cell label readable); skipped METRICS_PARAM_KEYS in `render_ceiling_yaml`'s loop so the ε kernel parameters never pollute the rendered ceiling YAML; plumbed a `metrics_params` argument through `run_one` that appends `-p key:=value` to the metrics cmd's `--ros-args`; extracted the per-cell metrics_params dict in `main`. Total ~15 lines added across the file, no breaking changes — every existing experiment (01 / 01b / 01c / 01d / 02a) reuses the same runner without modification because METRICS_PARAM_KEYS membership only triggers the new behaviour when a config actually includes one of those axes.

- *02b config + README + postprocess landed in the established 02a-shaped pattern.* `sim_validation_02b/lambda_sweep.yaml` is a 4-cell matrix over λ ∈ {1.0, 2.0, 3.0, 4.0} at production framework parameters (cs=0.10, snf=0.05, es=true, wnt=20, tv=0.0001 — identical to 02a / 01d's tv=0.0001 cell), with the λ=3 cell serving as the in-sweep reproducibility check against archived 02a (sub-mm on RMSE, ±5 % on σ², ±5 % on `tightening_post_init_mean`). README names the falsifiable predictions (exact λ-linearity, per-cell quantile cross-check, per-cell Jensen bound, in-sweep reproducibility, per-cell sanity), the falsification cases, the operating point, the read-out, the ~36 min wall cost, and the explicit scope-of-not (δ_cal and ε_base stay at defaults; framework parameters held constant). Postprocess produces six verdict blocks: in-sweep reproducibility, per-cell quantile cross-check, per-cell Jensen bound, per-cell consistency, cross-cell linearity (the headline structural gate), and the headline sensitivity figure table.

- *Dry-run verification: the four rendered ceiling YAMLs are byte-identical across the sweep, and the λ=3 cell's YAML is byte-identical to archived 02a's.* This is the strongest possible runtime guarantee that λ is only entering through the metrics cmd's `-p lam:=...` argument and not through any side channel into the elevation pipeline. The labels carry the expected `lam10/20/30/40` encoding. No 'lam' string appears in any rendered ceiling YAML — confirms the METRICS_PARAM_KEYS skip in `render_ceiling_yaml` is working as designed.

- *Postprocess smoke-test against synthetic data exercises all three exit paths.* Built a 4-cell synthetic result with closed-form-correct ε quantiles and tightening means at a fixed slope, all gates PASS, exit 0. Poisoned λ=2's `tightening_post_init_mean` by +10 % → linearity gate fired (slope at λ=2 = 155.5 mm vs median 141.4 mm, +10.0 % rel dev, exceeded the 5 % tolerance), exit 2 (structural FAIL). Restored λ=2, poisoned λ=3's RMSE_all → in-sweep reproducibility FAIL only (structural still PASS), exit 3. All three exit paths (0 / 2 / 3) verified before any real-bag launch. The structural-FAIL message correctly points at the three diagnosis hypotheses (wire-in not reading λ, σ² drifting, kernel regression).

- *02 §Implementation status caught up to reality.* Added a fifth block under §Implementation status covering the prototype kernel + 02a result + queued 02b. Per the impl-status-sync discipline, this update is overdue — the kernel landed 2026-05-26, the wire-in landed 2026-05-26, and 02a's headline number landed 2026-05-26, but only ADR 0010 and the journal carried that state until now. The new block also flags that the C++ runtime kernel inside `ceiling_constraint_field/syncCallback` will mirror the reference path's math, with parity as the regression discipline per ADR 0010 §Consequences.

**Didn't.**

- *Did not launch 02b.* Per the established pattern (01c / 01d / 02a, all launched by the user), the runner is ready and the user fires it. Headline number to read off: the median slope `E[√σ²_post_init]` in mm — that's the property of the sensor + framework + cadence regime that decouples from λ (a design choice) and δ_cal (yet to be calibrated). If the gates all PASS, the per-λ tightening table is the thesis sensitivity figure for 02 §Theory's chance-constraint formulation.

- *Did not extend the runner to pass-through δ_cal or ε_base in this session.* The plumbing is there (both are in METRICS_PARAM_KEYS and both have encoders); 02b's matrix only sweeps λ to isolate the coverage-parameter response from the calibration offset. A future 02c or partner-facility-calibration experiment can sweep `delta_cal` from the YAML using exactly the same plumbing.

- *Did not write a new ADR.* 02b is a direct application of the existing 02 §Theory + ADR 0010's runtime/prototype split + ADR 0009's variance-pipeline diagnosis. No subtle calls warrant an ADR — the closed-form λ-linearity prediction is in 02 §Theory, the runner extension is mechanical, the acceptance gates follow the 02a pattern verbatim. If the launch produces a slope-invariance FAIL with no obvious wire-in cause, an ADR captures the diagnosis at that point; not before.

- *Did not also audit 01b / 01c / 01d postprocess scripts for the silent-PASS pattern that R-2 caught in 02a.* The 02a diagnosis flagged this as a likely latent bug in the earlier reproducibility readers (None reads silently producing "+0.0 mm" PASSes). Worth checking but separate from 02b — the 02b postprocess is fresh code and was smoke-tested explicitly. Defer the audit pass to a separate session.

- *Did not start the phase-2 C++/CUDA port.* That was the other Path A option from the 2026-05-26 close-out question. The 02b path was chosen first; the port lands next, with the prototype kernel as the formula-of-record under the ADR 0010 parity discipline.

**Surfaced.**

- *The "skip in YAML render + plumb to runtime cmd" pattern generalises to any runner axis that targets a downstream tool's runtime params instead of a renderable config file.* The runner's existing `NON_ELEVATION_KEYS` skip-set handled splitter knobs (excluded from the ceiling YAML but consumed elsewhere); `METRICS_PARAM_KEYS` is structurally the same pattern applied to the metric script's ROS-param surface. Both are exceptions to the default "axis value goes into the rendered ceiling YAML" rule — recording the pattern explicitly here so a future axis that targets, say, the splitter's runtime params (rather than its z_low/z_high which already get picked up by the ceiling launch wrapping) can be added by extending one set without rebuilding the runner's flow.

- *The dry-run "rendered configs are byte-identical" check is the cheapest structural guarantee of a parameter-routing decision.* The whole point of routing λ through the metric script's cmd instead of the ceiling YAML is to keep the elevation pipeline 100 % identical across cells; comparing the rendered YAMLs byte-for-byte (and against an archived non-02b config) verifies this in one diff. Worth recording: any future routing decision in the runner that claims "this parameter doesn't affect X" can be verified by rendering both sides and diffing them. Stronger than reading the code path because it catches any unexpected side-effect of the parameter on the rendered output.

- *Smoke-testing all three exit codes (PASS / structural FAIL / reproducibility FAIL) on synthetic data before a real launch is cheap insurance against postprocess regression.* 02a's R-2 bug landed because the silent "+0.0 mm PASS" wasn't caught — the postprocess was never exercised against a known-good and known-bad input before the real bag ran. 02b's postprocess was smoke-tested in three states; the diagnosis text on the linearity-FAIL output reads as actionable on first read. Worth carrying as a working pattern: any new postprocess gets a synthetic PASS test + at least one synthetic FAIL test before the real-bag launch, with the FAIL state covering the gate the postprocess is most novel about (here: cross-cell linearity).

- *The slope `E[√σ²_post_init]` is the right thesis citation, not the per-λ tightening number.* 02a's headline citation was `tightening_post_init_mean = 424 mm at λ=3` — true but compound: it bundles a sensor-and-framework property (E[√σ²]) with a design choice (λ). 02b's headline is the slope itself: "the variance-aware kernel demands an extra ~140 mm of margin per unit of one-sided coverage parameter, at production framework parameters." That number is portable across thesis sections — chance-constraint sensitivity, calibration-corpus design, controller-side feasibility analysis — because it isolates the perception-side contribution from the chance-constraint design choice. The per-λ tightening numbers ({141, 283, 424, 566} mm if the linear-scaling holds) are the verification table; the slope is the citable number.

**Changed.**

- `src/thesis/experiments/runners/sim_validation_01_joint_sweep.py` — added `METRICS_PARAM_KEYS = {"lam", "delta_cal", "eps_base"}` constant; extended `_LABEL_ENCODERS` with `lam`/`delta_cal`/`eps_base` encoders; skipped METRICS_PARAM_KEYS in `render_ceiling_yaml`; plumbed `metrics_params` argument through `run_one` that appends `-p key:=value` to the metrics cmd's `--ros-args`; extracted per-cell metrics_params in `main`. Total ~15 lines, no breaking changes.
- `src/thesis/experiments/configs/sim_validation_02b/lambda_sweep.yaml` (new) — 4-cell λ-sweep at production framework parameters with quantified acceptance gates (in-sweep reproducibility ±5 mm / ±5 %, per-cell quantile cross-check ±1 mm, per-cell Jensen slack ±5 mm, consistency ±0.5 %, cross-cell linearity ±5 % relative, per-cell sanity [20, 800] mm).
- `src/thesis/experiments/configs/sim_validation_02b/README.md` (new) — falsifiable predictions, falsification cases, operating point, read-out, ~36 min cost, scope-of-not.
- `src/thesis/experiments/runners/sim_validation_02b_postprocess.py` (new) — six verdict blocks (in-sweep reproducibility, per-cell quantile, per-cell Jensen, per-cell consistency, cross-cell linearity, headline sensitivity figure). Exit code split: 0 PASS / 2 structural FAIL / 3 reproducibility FAIL. Smoke-tested across all three exit paths against synthetic data.
- `src/thesis/docs/02_variance_aware_clearance.md` §Implementation status — added a fifth block covering the prototype kernel + 02a result + queued 02b, per the impl-status-sync discipline (overdue update from 2026-05-26).
- `src/thesis/journal/2026-W22.md` — this entry.

02b is ready to launch — runner extension verified at dry-run, postprocess verified at smoke-test across all three exit paths, doc state caught up. The next experiment in the 02-stage chain is the chance-constraint coverage-parameter sensitivity figure, queued at ~36 min wall cost. After that lands, the phase-2 C++/CUDA port into `ceiling_constraint_field/syncCallback` is the natural next path; the prototype kernel is the formula-of-record, ADR 0010 §Consequences names parity as the regression discipline.

## 2026-05-27 (just past midnight, 02b ran 6/6 PASS — sim-phase arc closed)

**Tried.** User launched 02b at the close of 2026-05-26 (per the user-launches-the-bag pattern from 01c / 01d / 02a); ~36 min wall + ~30 s overhead; postprocess landed shortly after midnight. Read off the headline number and update 02 §Implementation status from "queued" to "complete + result", per the impl-status-sync discipline.

**Worked.**

- *6/6 PASS, exit 0.* All five structural gates (in-sweep reproducibility, per-cell quantile cross-check, per-cell Jensen bound, per-cell consistency, cross-cell linearity) plus the headline sensitivity figure all clear. The chance-constraint linearity in λ is empirically confirmed end-to-end through the script's wire-in on production-bag data, not just at the unit-test level.

- *Headline thesis number: `E[√σ²_post_init] = 138.45 mm` (median across the four cells, spread 2.38 % rel against the 5 % gate).* That is the property of the sensor + framework + production-cadence regime that decouples from both λ (a design choice) and δ_cal (yet to be calibrated). Variance-aware ε demands ~138 mm of extra margin per unit of one-sided coverage parameter on well-measured ceiling surfaces. The per-λ tightenings come out at {138.5, 281.5, 412.3, 553.7} mm against the closed-form predictions {138.5, 276.9, 415.3, 553.8} mm, all within ±1.7 % of the median-slope prediction.

- *In-sweep reproducibility against archived 02a: RMSE Δ = +0.0 / +0.2 mm (sub-mm), σ² post-init mean +4.4 % rel (inside the ±5 % gate, mostly the same cadence-driven jitter measured between 01d and 02a), tightening_post_init_mean at λ=3 = 412.3 mm vs archived 424.2 mm (−2.8 % rel, inside the gate).* The runner's `-p lam:=...` pass-through is structurally transparent — the λ=3 cell with the new plumbing reproduces archived 02a's defaults-only run to bag-replay determinism. METRICS_PARAM_KEYS works as designed; no silent regression introduced by the runner change.

- *λ=2 cell shows the largest slope deviation (+1.65 % vs median 138.45 mm) but is comfortably inside the gate.* Its σ² post-init mean is also the highest (0.02708 vs the others' ~0.0260) — the same cadence-driven σ² variation that 02a-vs-01d exposed (1.4 % over a 5-day stretch) bleeds into ~2 % slope variation across these four cells, by simple `√σ²` arithmetic. Nothing structurally suspicious. The slope spread (3.30 mm = 2.38 % rel) is at the level expected from σ²-reproducibility alone; tighter would be implausible.

- *ε quantile cross-check at ±0.000 mm across all 4 cells × 5 quantiles = 20 paired identities.* The closed-form `ε_q − ε_static = λ √σ²_q` matches to float precision at every λ — the wire-in's parameter reads, subset selection, and kernel invocation are all faithful to the formula-of-record. Same gate 02a passed at λ=3; now confirmed at λ ∈ {1, 2, 3, 4} too.

- *Module 02's sim-phase characterisation arc closes here.* Three load-bearing measurements on the same persistent bag, three different parameter axes: 01c (geometry), 01b (suppression threshold), 02a + 02b (chance-constraint kernel applied to the dual-instance σ² in the production regime). One structural finding (ADR 0009's √-floor / outlier-injection-floor at production), one structural confirmation (02b's λ-linearity at fixed framework parameters). The variance-aware ε construction stands on data; the remaining open question (δ_cal calibration corpus) is hardware-bound per 02 §Open questions/Hardware-deferred, not sim-bound.

**Didn't.**

- *Did not chase the λ=2 slope deviation further.* The +1.65 % is inside the gate, traceable to σ²-cadence variation (the λ=2 cell's σ² post-init mean is 4.4 % above the median), and not structurally suspect. Re-running would burn ~36 min for a result that's within noise; the across-cells slope spread of 2.38 % is the actual measurement noise floor for this experiment family, not a property of any single cell.

- *Did not write an ADR for the result.* 02b is a confirmation of the structural prediction 02 §Theory makes; there is no subtle call worth its own ADR (per [[feedback-adr-for-subtle-calls]]). The result lives in the experiment artefacts + the impl-status block + this journal entry.

- *Did not start phase-2 C++/CUDA port work.* That is the next session's first item — the prototype kernel is the formula-of-record, parity is the regression discipline per ADR 0010 §Consequences, and the structural gates 02b just cleared (closed-form quantile identity to float precision, linearity to 2.38 % rel) are exactly the kind of regression checks the C++ port will need to clear on top of the prototype's unit tests.

**Surfaced.**

- *The cross-cell slope `E[√σ²_post_init]` is a better thesis citation than any single per-λ tightening.* The slope isolates the sensor + framework contribution (the property the variance-aware kernel reads) from the chance-constraint coverage parameter (a design choice) and the calibration offset (yet to be fitted). Portable across thesis sections — chance-constraint sensitivity, calibration-corpus design, controller-side feasibility analysis — all consume the slope; per-λ numbers are the verification table, not the citable claim. The 02b headline-sensitivity-figure table renders both, but the load-bearing number is the slope. Worth carrying forward for future module documentation.

- *The σ²-reproducibility floor at ~1-2 % rel (cadence-driven) propagates through the kernel as ~2 % rel on E[√σ²].* `√σ²` is concave so the slope spread inherits the σ² spread compressed by the √. Empirically: σ² post-init mean spread across 02b cells is 4.4 % (max − min, vs median 0.0265); slope spread is 2.38 % rel. Roughly the half-power propagation Jensen would predict. Worth recording as the across-cell reproducibility floor for any future ε-aware experiment on the same bag — gates tighter than ~2 % rel on the slope are not meaningful at this measurement noise level.

**Changed.**

- `src/thesis/experiments/results/sim_validation_02b/lambda_sweep__5dc2888bd142_d34c74e/sweep_summary.md` — appended postprocess block (six verdicts, all PASS).
- `src/thesis/docs/02_variance_aware_clearance.md` §Implementation status — rewrote the "queued 2026-05-26" paragraph as "ran 6/6 PASS 2026-05-27 — sim-phase arc closed" with the headline slope, per-λ table, and reproducibility numbers. The phase-2 C++/CUDA port is the next item; the impl-status block names it explicitly.
- `src/thesis/journal/2026-W22.md` — this entry.

The variance-aware ε construction is sim-phase complete. The next session opens on the C++/CUDA port into `ceiling_constraint_field/syncCallback`, with the Python/CuPy prototype as the formula-of-record under the float32-ulp parity-as-regression discipline of ADR 0010 §Consequences.

**End of W22.** Week delivered sim_validation_01c + 01d + 02a + 02b (four experiments closing the sim-phase characterisation of modules 01 + 02), ADRs 0009 (variance-pipeline-α_d-diagnostic) and 0010 (clearance-field package boundary), the Python/CuPy prototype kernel + 13 passing tests, the compute_ceiling_metrics ε wire-in, three new repos (`hilda_clearance_field`, `hilda_nmpc`, `hilda_irm`) initialised under `oscardvs/`, and the thesis-citable slope `E[√σ²_post_init] = 138.45 mm` at production cadence. Open at week's close: phase-2 C++/CUDA port; partner-facility δ_cal corpus (hardware-bound). The colloquium-side narrative arc for G2 is sim-phase intact.

## 2026-05-27 (postprocess silent-PASS audit)

**Tried.** The 02a R-2 finding ("a silent failing PASS is worse than a vocal FAIL", journal 2026-05-26 late-evening) named the five postprocess scripts as the likely systematic-debugging hot spot. Sweep 01b / 01c / 01d / 02a / 02b for the same class of bug before opening the C++/CUDA port — the port adds wire-in surface and any latent silent-skip would compound there.

**Worked.**

- *Audit findings.* The exact 02a R-2 schema-mismatch (nested `summary.get("all").get("rmse")` against the runner's flat `RMSE_all`) is absent everywhere else — schema reads were correct in 01b/01c/01d. But a related class of bug was present in every script: silent-default-True on missing fields, where an auxiliary check that couldn't run because its inputs were absent contributed `True` to the overall `passed` flag without surfacing the skip. Six concrete sites: 01d reproducibility's variance sub-check defaulting `var_pass=True`; 02a reproducibility's variance sub-check ditto; 02a quantile cross-check silently `continue`-ing past missing quantiles without setting `all_pass=False`; 02a Jensen-bound returning `(lines, True)` on missing fields; 02b in-sweep reproducibility's variance + tightening sub-checks; 02b per-cell Jensen / consistency / headline / linearity all silent-skipping missing per-cell fields with no impact on `all_pass`. The discipline lesson the journal flagged on 2026-05-26 ("refuse to print PASS when inputs are None") was a recurring pattern, not isolated to 02a.

- *Fix policy chosen.* Two rules. (a) When the runner is expected to produce a field, missing → fail loudly (since absence means the wire-in or projection step is broken — that's the 02a R-3 class). (b) When a sub-check is genuinely optional (e.g., variance reproducibility on top of RMSE reproducibility, the variance fields might pre-date the runner gaining variance pass-through), missing → don't claim PASS for it; surface "(σ² check skipped — fields missing)" in the verdict line so a markdown reader can't misread silence as success. The full-block skip path (archive not loadable) gets a new explicit `Verdict: **SKIPPED**` line so the markdown is unambiguous; the function still returns True so the exit code stays 0 (no archive ≠ failure).

- *Patches applied across all five scripts.* Each silent-skip site rewritten to either count as fail-loud (when the field is wire-in-mandatory) or to set a `var_ran` / `tight_ran` flag and surface the skip in the verdict text (when the sub-check is auxiliary). `_fmt_mm(None)` already returned "—"; analogous "N/A" handling added to the relative-Δ formatting where missing inputs land. The `Verdict:` line in every block now either says PASS, FAIL, SKIPPED, or PASS-with-explicit-skip-note — never bare PASS when an input was None.

- *Verification — happy-path PASS unchanged, SKIP/FAIL paths produce correct verdicts.* Pre-audit snapshot of all five `sweep_summary.md` files taken before any edits. Re-ran each postprocess against its archived result dir — all five returned exit 0 (happy-path PASS preserved). Smoke-tested every SKIP / missing-field path against synthetic input through a direct-call test of each affected function; eleven assertions across the five scripts all pass. The exit-code regression surface is contained: archived runs continue to PASS, only newly-introduced missing-data paths now fail loudly.

- *Marker-block fragility surfaced.* The re-run dropped a hand-written "Diagnostic resolution" addendum that was placed *inside* the `BEGIN`/`END` postprocess markers of 01c's sweep_summary, and repositioned 01d's reconciliation note (placed *after* the END marker, so it survived but moved). Restored both from the preaudit snapshots — the new postprocess code, run on the archived data with the existing variance fields present, produces output byte-equivalent to the preaudit except for a single whitespace difference in 02a, so the restoration loses nothing. Worth flagging: hand-written content placed between the postprocess markers is fragile to any future re-run; future addenda should land *outside* the marker pair (either before BEGIN or after END), or be promoted into the postprocess source as part of the auto-generated block.

**Didn't.**

- *Did not refactor the five scripts to share a common verdict-helper module.* Each script now repeats the same skipped-vs-failed verdict-formatting idiom inline; a shared `postprocess_verdicts.py` would deduplicate but is out of scope for a latent-bug fix. Promote if the C++/CUDA port adds another postprocess that wants the same shape.

- *Did not commit the fixes yet.* One focused commit covering the five script edits + this journal entry. The runner's `metrics_params` plumbing change from 02b is already on disk under `6566447` so this commit stays narrow.

- *Did not also extend the postprocess to refuse to clobber inside-marker hand-written content.* The right fix is either to detect non-whitespace lines inside the marker pair and emit a warning, or to migrate the marker pair to a pure machine-managed region with hand-written addenda landing in a different file. Out of scope; flagged here so the next addendum doesn't repeat the same mistake.

**Surfaced.**

- *The discipline rule generalises beyond postprocess scripts.* Any pass/fail aggregation that consumes multiple sub-checks with optional inputs faces this footgun. The right shape is: every sub-check declares whether it ran, the overall verdict is conditional on (i) what ran AND passed, (ii) what didn't run, and (iii) what's marked as "must run". Variants of this pattern will show up in the C++/CUDA port's parity tests (the prototype's `epsilon_only` and `epsilon_stats` paths must both run, plus a per-element-comparison; a silent skip on the comparison would be the same class of bug). Worth pre-committing the discipline before the port wires it in: parity tests fail-loud on any missing path, not silent-pass on partial output.

- *Marker-managed regions and hand-written content don't mix.* The five postprocess scripts use a BEGIN/END marker pair to make their write idempotent — the script strips its prior block before re-appending. This loses content placed inside the markers between runs. The 01c diagnostic resolution and 01d reconciliation note were exactly this: hand-written audit notes added to the rendered output, intended to survive across re-runs but in fact load-bearing on the original file timestamp. The discipline: when the postprocess output needs hand-annotation, place the annotation *outside* the markers (the script's concatenation logic preserves before-marker and after-marker content, just repositioning the latter). Future audit annotations of postprocess results follow this convention.

**Changed.**

- `src/thesis/experiments/runners/sim_validation_01b_postprocess.py` — `reproducibility_block`'s archive-missing and RMSE-missing branches now emit explicit `Verdict: **SKIPPED**` / `Verdict: **FAIL**` lines.
- `src/thesis/experiments/runners/sim_validation_01c_postprocess.py` — same two-branch verdict-line fix.
- `src/thesis/experiments/runners/sim_validation_01d_postprocess.py` — same archive/missing fixes; variance sub-check inside reproducibility now tracks `var_ran` and surfaces "(σ² check skipped — variance fields missing)" in the verdict when its inputs are absent, no longer silently contributes PASS.
- `src/thesis/experiments/runners/sim_validation_02a_postprocess.py` — reproducibility var sub-check tracks `var_ran` like 01d; quantile cross-check sets `all_pass=False` on any missing quantile (was `continue` without effect, plus reports the count of missing quantiles in the verdict); Jensen bound returns `(lines, False)` with explicit FAIL verdict when required fields missing (was `(lines, True)`).
- `src/thesis/experiments/runners/sim_validation_02b_postprocess.py` — in-sweep reproducibility tracks `var_ran` and `tight_ran` for the two auxiliary sub-checks; per-cell Jensen / consistency / headline / cross-cell linearity all set `all_pass=False` (or `all_in_range=False`) on missing per-cell fields and report the count of missing cells in the verdict line. Archive-not-loadable branch emits explicit SKIPPED verdict.

The latent-bug pass is closed: all five scripts now have the discipline "refuse to print PASS when inputs are None" and "print N/A not +0.0 mm" embedded structurally, not relying on the author noticing per-block. The next session opens on Path A — the C++/CUDA port into `ceiling_constraint_field/syncCallback`, with the parity-test discipline from ADR 0010 §Consequences as the regression gate and the just-applied fail-loud-on-missing convention as the postprocess discipline.

## 2026-05-27 (Phase A: variance-aware kernel ported to C++ + wired into the runtime node)

**Tried.** Path A from yesterday's close-out: port the variance-aware kernel from the Python/CuPy prototype into `hilda_ceiling/ceiling_constraint_field/`. Two-commit split — first commit the kernel + parity-tested gtest with the syncCallback TODO still in place, second commit the wire-in itself. This separates "math is correct" from "node integration uses it correctly" so each commit has a single failure mode.

**Worked.**

- *Header-only Eigen kernel landed.* `include/ceiling_constraint_field/variance_aware_kernel.hpp` mirrors the prototype's `variance_aware_field_reference` exactly. Uses `Eigen::Ref<MatrixXf>` so grid_map's MatrixXf layers pass without copying; the `.select((sigma2_sum < 0).select(zeros, sigma2_sum))` pattern preserves NaN through the negative-variance clamp (NaN < 0 is false → false branch returns the original NaN sum → sqrt(NaN) = NaN downstream). No CUDA dependency — the Eigen path is the natural shipping form at the 10 Hz publish cadence; the CuPy `ElementwiseKernel` from the prototype is the fused-GPU variant whose port is gated on G5 measurement showing Eigen-CPU is insufficient.
- *Six-case gtest covers the five Python prototype tests verbatim* — zero variance, closed-form λ√σ² scaling, sign of feasibility, NaN propagation per cell, negative-variance clamp — at the same numerical fixture (PARAMS = (0.85, 0.05, 0.02, 3.0), S = 0.40) plus a defensive "both layers negative" case. 6/6 pass at 1e-6 absolute tolerance. This is the float32-ulp parity gate ADR 0010 §Consequences names; the math is now redundantly defined in both implementations and each side's tests verify against the closed form rather than against each other (parity by transitivity, no cross-process golden-file dependency).
- *Syncing the kernel to ament_cmake_gtest discovered grid_map_core's Eigen plugin coupling.* Adding `find_package(grid_map_core REQUIRED)` at the top of the CMakeLists adds global compile definitions like `-DEIGEN_MATRIXBASE_PLUGIN=<grid_map_core/eigen_plugins/MatrixBasePlugin.hpp>`. When the test target links Eigen3::Eigen but doesn't carry grid_map_core's include path, the plugin header is unfindable and the build dies with "no such file". Fix: `ament_target_dependencies(test_variance_aware_kernel grid_map_core)` propagates the include path. Even though the kernel itself depends only on `<Eigen/Core>`, the build environment isn't pure Eigen. Worth recording — any future thesis-stage C++ component that wants standalone Eigen tests inside a package that also uses grid_map needs this propagation.
- *Node wire-in lands as a clean syncCallback replacement.* The skeleton's TODO becomes: `fromMessage` for each input GridMap with throttled-warn-on-failure; presence check for `elevation` + `variance` layers in either map (throttle-warn + skip if absent); cell-geometry alignment check (size, resolution, origin within 1e-6 m; throttle-warn + skip with the deltas in the message if mismatched); build a four-layer output GridMap matching the ceiling's geometry; call the kernel; publish. The fail-loud-not-silent discipline from this morning's postprocess audit applies on the C++ side: misaligned or incomplete inputs never produce garbage on `/constraint_field`, and the operator gets a specific message about which knob is off rather than a vague generic warning.
- *Parameter surface gains δ_cal, λ, s plus configurable layer names.* The previous `eps_safety` parameter was a single static safety margin; per the formula-of-record it's now `eps_base` (the static baseline component of ε) with the variance-aware addition `δ_cal + λ√σ²_c` computed per cell from the inputs. Existing launch configs that pass `eps_safety: 0.05` will need a one-line rename; the default is unchanged so behaviour at the static-margin baseline is preserved. The `s` parameter (sledge extension) is a static placeholder — production wiring subscribes to `/reference_controller/state` for the augmented-state value once the controller side lands. Layer names (`elevation_layer`, `variance_layer`) are configurable to absorb any upstream `elevation_mapping_cupy` rename.
- *Build clean, kernel 6/6 still green after the node compile.* The kernel header is touched in commit 1; commit 2 only modifies the cpp + hpp of the node. The gtest re-runs cleanly after commit 2 because the kernel header didn't change — confirming the wire-in didn't inadvertently break the formula-of-record.

**Didn't.**

- *Did not port to CUDA.* The Eigen-CPU path is sufficient for the 10 Hz publish cadence (n_cells per cycle is modest — a typical 40×40 map at 0.10 m resolution is 160 k cells, and the kernel is purely elementwise so Eigen auto-vectorisation handles it). The fused CUDA kernel from the prototype's `ElementwiseKernel` is reserved for the G5 latency-under-load measurement on Jetson; if Eigen-CPU lands inside the perception+controller budget, no port is needed. Phase A scope is "production runtime publishes variance-aware field"; whether it's CPU or GPU is a profiling decision, not a discipline decision.
- *Did not implement the CeilingClearance-at-robot-pose message.* The existing skeleton's `clearance_pub_` is preserved and the topic name parameter still declared, but nothing publishes on it yet. The message wants the robot's (x, y) in the ceiling-map frame to look up the per-cell clearance and feasibility, which needs a TF lookup or a subscription to a pose topic. Out of scope this session; lands in a follow-up that touches only the publish path, not the kernel or the field computation.
- *Did not update launch/config files for the renamed eps_safety → eps_base parameter.* The change is backward-incompatible at the YAML level but the per-cell behaviour at the default value is unchanged. A grep of the workspace's `*.launch.py` and `config/*.yaml` for `eps_safety` will surface the call sites; the update is mechanical (~5 lines) and lands when next someone runs the node in sim and sees the deprecation warning.
- *Did not run the node end-to-end against a rosbag.* Build + unit-test pass; the runtime exercise (subscribe to actual `/floor_elevation_mapping_node/elevation_map_raw` + `/ceiling_elevation_mapping_node/ceiling_map_raw`, observe `/constraint_field` publishing) is the next thing to do. The persistent sim bag from 01x / 02x is the natural test case; replay it and check that the four output layers populate with sensible values + tightening_post_init_mean reproduces the 424 mm 02a number at λ=3.

**Surfaced.**

- *The two-commit split (kernel-only, then wire-in) is the right shape for a port that mirrors a formula-of-record.* Commit 1's test discipline is purely about the math (closed-form identities); commit 2's discipline is purely about message-handling correctness. Combined, the diff would be ~470 lines across 5 files and the review surface would mix two distinct kinds of correctness; split, each commit has a single review axis. The parity discipline ADR 0010 §Consequences names is the natural cut point — once commit 1 passes its 6/6 gtest, the math is settled and commit 2 can't accidentally break it without uncrustify/build failing. Worth keeping as a pattern for the inevitable HMPC controller port and any future kernel-with-runtime-node pair.
- *grid_map_core's Eigen-plugin coupling is a footgun for standalone tests in a grid_map-using package.* Documented in the CMakeLists comment so the next reader doesn't spend ten minutes diagnosing the same "no such file" build error on a fresh checkout. The root cause is upstream design (grid_map_core injects Eigen plugins globally rather than through its own include scope), so the workaround is to propagate the include path even to targets that don't use grid_map directly.
- *The `eps_safety → eps_base` rename is a small example of "naming follows the formula-of-record".* The parameter was named for what it did in the old single-static-margin world; in the new variance-aware world it's the *baseline* component (one of three terms in ε), not the full margin. Renaming at port time keeps the param surface aligned with what the code actually computes, even though it's a one-line breaking change at the YAML level. The alternative (keep `eps_safety` as an alias for `eps_base`) would carry the wrong mental model into every future launch config.
- *Fail-loud-not-silent applies to runtime nodes as much as to postprocess scripts.* The morning's postprocess audit found six silent-default-True sites; the wire-in commit lands the same discipline on the C++ side: layer-missing → throttle-warn + skip, geometry-mismatch → throttle-warn + skip with deltas. The runtime never publishes garbage from incomplete inputs, and the operator gets actionable diagnostic text rather than a silent absence-of-output. Same shape of bug, same shape of fix, applied at two different layers of the pipeline.

**Changed.**

In `hilda_ceiling/gazebo` (two commits):
- `ceiling_constraint_field/include/ceiling_constraint_field/variance_aware_kernel.hpp` (new) — header-only Eigen kernel with `FieldParams<T>` struct and `compute_variance_aware_field()` function mirroring the prototype's reference path.
- `ceiling_constraint_field/test/test_variance_aware_kernel.cpp` (new) — 6-case gtest harness covering the prototype's five Python tests at identical numerical fixture plus a defensive double-negative case.
- `ceiling_constraint_field/CMakeLists.txt` — `ament_add_gtest` target with grid_map_core dependency to propagate the Eigen-plugin include path.
- `ceiling_constraint_field/package.xml` — `<test_depend>ament_cmake_gtest</test_depend>`.
- `ceiling_constraint_field/include/ceiling_constraint_field/constraint_field_node.hpp` — gained `delta_cal_`, `lam_`, `s_`, `elevation_layer_`, `variance_layer_` member vars; renamed `eps_safety_` to `eps_base_`.
- `ceiling_constraint_field/src/constraint_field_node.cpp` — syncCallback implemented: fromMessage with throttled-warn-on-failure, layer-presence + geometry-alignment checks, kernel call, four-layer GridMap publish. New parameter declarations (`delta_cal`, `lam`, `s`, `elevation_layer`, `variance_layer`); `eps_safety` rename to `eps_base`.

In `thesis/main` (this entry):
- `src/thesis/docs/02_variance_aware_clearance.md` §Implementation status — added the "C++ kernel + node wire-in landed" block under the sim-phase-arc-closes paragraph; named the CUDA-port deferral and the CeilingClearance message TODO.
- `src/thesis/journal/2026-W22.md` — this entry.

Phase A is sim-phase complete on the C++ side. The runtime node now publishes the variance-aware constraint field from the dual-instance elevation maps; the formula-of-record is parity-tested at float32-ulp; the validation discipline from the postprocess audit is mirrored on the runtime path. Next: runtime exercise against the persistent sim bag, then either the CeilingClearance follow-up or the CasADi B-spline export from `hilda_clearance_field/`, whichever the controller side wants first.

## 2026-05-27 (Phase A verification: bag replay surfaces geometry-tolerance issue)

**Tried.** Push the four commits to remotes (thesis/main + hilda_ceiling/gazebo). Then bag replay verification: orchestrate `constraint_field_node` and `ros2 topic hz /constraint_field` as background sidecars while the existing `sim_validation_02a` runner plays the persistent bag in the foreground. The runner doesn't know about the new node — its job is the elevation pipeline + metrics — but the node, started before the runner, subscribes to the elevation topics as they come up and publishes `/constraint_field` for the bag duration. Expected outcome: ~10 Hz on `/constraint_field`, no warnings, a 5/5 PASS on the regression 02a result.

**Worked.**

- *Pushes clean.* `thesis/main 194189e..c62acf7` and `hilda_ceiling/gazebo b8ce4d7..52fdc70`. Both private repos under `oscardvs/`. The kernel + wire-in are now durable artefacts.
- *Wire-in plumbing works.* The constraint_field_node logs `ConstraintFieldNode initialised (h_base=0.85 eps_base=0.050 delta_cal=0.000 lam=3.0 s=0.00)` on startup, then begins receiving synchronised GridMap pairs from the runner-spawned floor + ceiling elevation nodes within the first 2 s. The kernel runs successfully on the cycles that pass the geometry check; `/constraint_field` publishes at a steady 5.000 Hz throughout the bag (10 Hz on inputs, half the cycles pass the geometry check). No segfaults, no `fromMessage` failures, no layer-missing warnings.
- *Regression check on the elevation pipeline is clean.* The runner produced a new result dir `variance_aware_epsilon_baseline__5abb8400ad08_c62acf7/` separate from the archived `__5abb8400ad08_d34c74e/` (git_sha changed), so the original archive was preserved. The new 02a postprocess returns 5/5 PASS: RMSE_all Δ = +0.1 mm, RMSE_core Δ = +0.2 mm, σ² post-init Δ = 2.8 % (inside ±5 %), ε quantile cross-check ±0.000 mm at every quantile, Jensen gap +71.9 mm, `tightening_post_init_mean = 412.1 mm` (vs archived 424.2 mm, −2.8 % rel — driven by the cadence-jitter floor [[variance-layer-regime]] documents). Everything the runner measures is regression-stable; the c62acf7 commits don't break the elevation pipeline anywhere.

**Didn't.**

- *Did not catch the geometry-tolerance issue at unit-test time.* The kernel's gtest verifies the math on synthetic 8×8 inputs where the two layers are perfectly aligned by construction. The wire-in's geometry-alignment check is unit-testable in isolation but no test was written for it; the bag-replay exercise was the first contact with real GridMap geometry-drift conditions.
- *Did not yet run the variance-aware ε kernel on every elevation cycle.* The 1e-6 m position tolerance is too strict for the actual sim conditions: floor and ceiling elevation_mapping_cupy instances each maintain their own `getPosition()` via independent odom callbacks, and the two positions drift sub-cell-scale between updates (observed deltas range from displayed-as-0.0000 m up to 0.0633 m, ~0.6 cells at 0.10 m resolution). The check rejects ~half the synced pairs and the throttled-warn pattern from the postprocess audit translates into ~14 warnings logged over 9 minutes (one every ~38 s on average), each reporting the specific delta. The fail-loud discipline correctly surfaced the issue — the warning text named the size, resolution, and pos delta exactly, so the diagnosis was immediate.
- *Did not investigate the upstream root cause of the floor/ceiling position drift.* The likely mechanism is that the two elevation node instances run as separate processes with independent odom callback schedulers; between odom messages, only one instance updates its `position_` before the next map publish. Resolving this upstream would require changes in `elevation_mapping_cupy` (e.g., a shared pose lookup or synchronised move triggers). Not in scope this commit; the lenient tolerance below is the sim-phase fix.

**Surfaced.**

- *The fail-loud discipline did its job.* The geometry-mismatch warning surfaced a real issue immediately — no silent failure, no garbage on `/constraint_field`, and the operator gets the size/resolution/delta in the warning text so the diagnosis is one read. If the threshold had been silently relaxed by default, the wire-in would have published subtly off-cell ε values without any indication that the inputs were misaligned. The 02a R-2 "silent PASS is worse than vocal FAIL" lesson generalises: a too-strict tolerance that surfaces real misalignments is the right default for first contact with new data, even at the cost of skipping cycles. Tighten the tolerance only after the misalignment regime is characterised, not before.
- *Sub-cell position drift on the dual elevation instances is a real-data finding worth recording.* The deltas (0–63 mm at 100 mm resolution) are below the ε margin's per-cell scale (~140 mm at production cadence per 02b's headline slope), so cell-wise composition with one-cell-tolerance still produces meaningful ε numbers, but the *spatial accuracy* of any individual cell is reduced by the drift. For sim-phase aggregate measurements (tightening_post_init_mean over thousands of cells) the drift averages out; for any per-cell verification (e.g., comparing the constraint_field_node's output cell (i,j) against the prototype's output at world location (x, y) of that cell), the drift matters. Production should resample one map onto the other's grid before per-cell composition. Add to the production-deployment punch list.
- *The bag replay exercised parts of the wire-in that the gtest can't.* GridMap deserialisation via `fromMessage`, layer-name lookup against the actual ROS message, ApproximateTime synchronisation between independent publishers, real-time clock interaction at 10 Hz. All of these are now empirically confirmed to work end-to-end. The 6-case gtest gives the math discipline; the bag replay gives the wire-in discipline. Both are needed.

**Changed.**

- `hilda_ceiling/ceiling_constraint_field/src/constraint_field_node.cpp` — position-tolerance changed from hardcoded 1e-6 m to `ceiling_map.getResolution()` (one full cell). Comment block explains the rationale + names the production-resampling deferral. The size + resolution checks stay strict.
- `hilda_ceiling/ceiling_constraint_field/config/constraint_field_params.yaml` — `eps_safety: 0.05` renamed to `eps_base: 0.05`; new entries `delta_cal: 0.0`, `lam: 3.0`, `s: 0.0`, `elevation_layer: "elevation"`, `variance_layer: "variance"`. Matches the formula-of-record parameter names; default behaviour at sim-phase values unchanged.
- `src/thesis/experiments/results/sim_validation_02a/variance_aware_epsilon_baseline__5abb8400ad08_c62acf7/` — new 02a regression result, 5/5 PASS, `tightening_post_init_mean = 412.1 mm`. Archived `__d34c74e` result preserved (different git_sha, different result_dir hash).
- `src/thesis/journal/2026-W22.md` — this entry.

Verification target after the re-run with the relaxed tolerance: `/constraint_field` at ~10 Hz (or close), no geometry warnings (or only the rare >1-cell case), kernel processing every synced pair. Records as the empirical sign-off on the wire-in once the re-run lands.
